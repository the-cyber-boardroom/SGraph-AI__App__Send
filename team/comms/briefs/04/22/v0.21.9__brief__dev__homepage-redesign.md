# Dev Brief: Homepage Redesign
**Version:** v0.21.9 | **Date:** 2026-04-22 | **From:** Designer + Ambassador
**Analysis document:** `team/comms/briefs/04/22/v0.21.9__analysis__homepage-combined-findings.md`
**Target file:** `sgraph_ai__website/v0/v0.2/v0.2.0/en-gb/index.html`
**CSS files:** `sgraph_ai__website/v0/v0.2/v0.2.0/_common/css/homepage.css` and `style.css`

---

## Scope

All changes are to the v0.2.0 homepage. No changes to the production `sgraph_ai__website/en-gb/index.html` or any application code. No mocks, no patches, no framework dependencies — vanilla HTML/CSS only, consistent with IFD methodology.

---

## Task 1 — Wire screenshots into use-case rows
**Priority:** P0 | **Effort:** 30 min | **File:** `en-gb/index.html`

Replace each `<div class="screenshot-placeholder">` with a `<figure class="use-case-figure">` containing an `<img>`.

Image path prefix (relative from `en-gb/index.html`):
`../_common/images/sg-send/use-cases/`

Mapping:
| Use case | Replace placeholder with |
|---|---|
| Security Research | `<img src="../_common/images/sg-send/use-cases/01-vault-browse.png" alt="Vault browse view showing OWASP articles" loading="lazy">` |
| Multi-Agent Workflows | `<img src="../_common/images/sg-send/use-cases/05-push-pull-badge.png" alt="Vault with push/pull commit model" loading="lazy">` |
| Healthcare | `<img src="../_common/images/sg-send/use-cases/08-token-receive.png" alt="Opening a vault with a share token" loading="lazy">` |
| Browser Automation | `<img src="../_common/images/sg-send/use-cases/04-upload-flow.png" alt="Upload and send flow" loading="lazy">` |
| Developer Workflow | `<img src="../_common/images/sg-send/use-cases/02-markdown-article.png" alt="Markdown article rendered in vault browser" loading="lazy">` |

Add to `homepage.css`:
```css
.use-case-figure {
  margin: 0;
  border-radius: var(--radius-card);
  overflow: hidden;
  border: 1px solid var(--border-color);
}

.use-case-figure img {
  width: 100%;
  height: auto;
  display: block;
}
```

---

## Task 2 — Fix encryption badge spacing
**Priority:** P1 | **Effort:** 5 min | **File:** `homepage.css`

In `homepage.css`, update the hero top padding and badge margin:
```css
/* reduce the dead space above the enc-badge */
.hero {
  padding-block-start: var(--sp-16);   /* was var(--sp-24) in style.css */
}

.enc-badge {
  margin-bottom: 12px;   /* was 24px */
}
```

Note: `style.css` sets `.hero { padding-block: var(--sp-24) var(--sp-16); }`. Override the start value in `homepage.css` to avoid editing the shared stylesheet.

---

## Task 3 — Replace 3rd primitive card with PKI
**Priority:** P1 | **Effort:** 15 min | **File:** `en-gb/index.html`

Replace the entire Code Execution Sandbox `<div class="primitive-card primitive-card--coming-soon">` block with:

```html
<div class="primitive-card">
  <span class="primitive-card__badge">Available now</span>
  <div class="primitive-card__icon">🔑</div>
  <h3 class="primitive-card__title">Cryptographic Identity</h3>
  <p class="primitive-card__desc">
    Every vault has its own key pair. Every commit is signed.
    Agents and humans have verifiable identities — trust without a central authority.
  </p>
  <ul class="primitive-card__features">
    <li class="primitive-card__feature">Per-vault key — not your GitHub credentials</li>
    <li class="primitive-card__feature">Commit signing — every action is attributable</li>
    <li class="primitive-card__feature">Scoped access — agents get a key, not your account</li>
    <li class="primitive-card__feature">Share tokens — read-only, point-in-time snapshots</li>
  </ul>
</div>
```

Move the Sandbox card content to a new "What's coming" callout after the primitives grid (before the `</section>` closing tag):
```html
<div class="callout mt-8">
  <strong>Coming soon:</strong> Code Execution Sandbox — isolated container per execution,
  code loaded from vault at runtime, output captured back to vault.
  Seven deployment targets, one codebase, same encryption everywhere.
  Lambda → Fargate → EC2 → Docker → GCP → CLI → Self-hosted.
</div>
```

---

## Task 4 — Add interaction patterns section
**Priority:** P1 | **Effort:** 45 min | **Files:** `en-gb/index.html`, `homepage.css`

Add a new section immediately after the closing `</section>` of the hero and **before** the AWS strip. This section visualises the three core interaction patterns using CSS-only layout (no SVG library, no images).

Add to `en-gb/index.html` after line 93 (after `</section>` of hero):

```html
<!-- ═══ INTERACTION PATTERNS ═══════════════════════════════════ -->
<section class="section section--alt">
  <div class="container">
    <div class="section-header">
      <h2>Three ways vaults are used</h2>
      <p>The same encrypted vault. Three interaction patterns. One token to share.</p>
    </div>
    <div class="patterns-grid">

      <div class="pattern-card">
        <div class="pattern-flow">
          <span class="pattern-actor pattern-actor--human">👤</span>
          <span class="pattern-arrow">→ vault →</span>
          <span class="pattern-actor pattern-actor--human">👤</span>
        </div>
        <h3 class="pattern-title">Human → Human</h3>
        <p class="pattern-desc">
          Send encrypted files to colleagues, clients, or researchers.
          The recipient opens with a share token — no account required.
        </p>
      </div>

      <div class="pattern-card pattern-card--featured">
        <div class="pattern-flow">
          <span class="pattern-actor pattern-actor--agent">🤖</span>
          <span class="pattern-arrow">→ vault →</span>
          <span class="pattern-actor pattern-actor--human">👤</span>
        </div>
        <h3 class="pattern-title">Agent → Human</h3>
        <p class="pattern-desc">
          An AI agent writes findings, reports, or briefings to a vault.
          A human opens it with a token — complete audit trail preserved.
        </p>
      </div>

      <div class="pattern-card">
        <div class="pattern-flow">
          <span class="pattern-actor pattern-actor--agent">🤖</span>
          <span class="pattern-arrow">→ vault →</span>
          <span class="pattern-actor pattern-actor--agent">🤖</span>
        </div>
        <h3 class="pattern-title">Agent → Agent</h3>
        <p class="pattern-desc">
          Agent A researches and writes to a vault. Agent B reads, analyses,
          and writes back. No shared credentials. Every action is a signed commit.
        </p>
      </div>

    </div>
  </div>
</section>
```

Add to `homepage.css`:
```css
/* Interaction Patterns */
.patterns-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--sp-6);
}

.pattern-card {
  background-color: var(--bg-surface);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-card);
  padding: var(--sp-6);
  text-align: center;
  transition: border-color var(--transition-normal);
}

.pattern-card:hover {
  border-color: var(--accent);
}

.pattern-card--featured {
  border-color: var(--accent);
  background-color: rgba(78, 205, 196, 0.04);
}

.pattern-flow {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--sp-3);
  margin-bottom: var(--sp-4);
  font-size: 0.85rem;
  color: var(--text-secondary);
}

.pattern-actor {
  font-size: 2rem;
  line-height: 1;
}

.pattern-arrow {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--accent);
  white-space: nowrap;
}

.pattern-title {
  font-size: var(--fs-h3);
  margin-bottom: var(--sp-3);
}

.pattern-desc {
  color: var(--text-secondary);
  font-size: 0.9rem;
  line-height: 1.6;
  margin-bottom: 0;
}

@media (max-width: 768px) {
  .patterns-grid {
    grid-template-columns: 1fr;
  }
}
```

---

## Task 5 — Add vault view modes to primitive card features
**Priority:** P1 | **Effort:** 10 min | **File:** `en-gb/index.html`

In the "Encrypted Virtual File System" card, replace the features list with:
```html
<ul class="primitive-card__features">
  <li class="primitive-card__feature">Each vault has its own key — not your GitHub credentials</li>
  <li class="primitive-card__feature">Every file operation is a commit — full audit trail</li>
  <li class="primitive-card__feature">Folder view, gallery view, markdown rendering — in-browser</li>
  <li class="primitive-card__feature">Three access modes: Online, CLI (sgit), API</li>
</ul>
```

---

## Task 6 — Add "No account required" to hero
**Priority:** P1 | **Effort:** 5 min | **File:** `en-gb/index.html`

Add a note immediately after `.hero-ctas` and before `.deploy-strip`:
```html
<p class="hero-note">No account needed to receive. Open any vault with a token.</p>
```

Add to `homepage.css`:
```css
.hero-note {
  margin-top: var(--sp-4);
  font-size: 0.85rem;
  color: var(--text-secondary);
}
```

---

## Task 7 — Move privacy section before pricing + visual elevation
**Priority:** P1 | **Effort:** 15 min | **Files:** `en-gb/index.html`, `homepage.css`

**Reorder sections:** Move the entire `<!-- ═══ PRIVACY INLINE ═══ -->` section (lines 390–414) to appear immediately before `<!-- ═══ PRICING TEASER ═══ -->` (currently line 369).

**Elevate the visual design** of `.privacy-inline` in `homepage.css`:
```css
.privacy-inline {
  border-left: 4px solid var(--accent);
  border-radius: 0 var(--radius-card) var(--radius-card) 0;
}

.privacy-inline__text {
  font-size: 1rem;        /* was 0.9rem */
  line-height: 1.9;       /* was 1.8 */
}
```

---

## Task 8 — Add Vaults to navigation + create vaults stub page
**Priority:** P1 | **Effort:** 30 min | **Files:** `en-gb/index.html`, new `en-gb/vaults/index.html`

**In the nav** (line 25–30), add a Vaults link:
```html
<nav class="site-nav" id="main-nav">
  <a href="how-it-works/">How it Works</a>
  <a href="vaults/">Vaults</a>
  <a href="security/">Security</a>
  <a href="https://tools.sgraph.ai" target="_blank" rel="noopener">Tools</a>
  <a href="pricing/">Pricing</a>
</nav>
```

**Create** `sgraph_ai__website/v0/v0.2/v0.2.0/en-gb/vaults/index.html` — a dedicated page covering:
- What a vault is
- Three access modes (Online / CLI / API)
- View modes (folder, gallery, markdown)
- The commit model
- Key types (vault key vs share token)
- Getting started with sgit CLI

**Also in the footer**, add Vaults under "Developers":
```html
<li><a href="vaults/">Vaults</a></li>
```

---

## Task 9 — Fix mobile token bar
**Priority:** P2 | **Effort:** 10 min | **File:** `style.css`

In the mobile media query for `.token-inline-bar` (around line 1321 of `style.css`), change:
```css
/* BEFORE */
@media (max-width: 768px) {
  .token-inline-bar {
    display: none;
  }
  ...
}

/* AFTER */
@media (max-width: 768px) {
  .token-inline-bar {
    display: flex;    /* always show the trigger */
    flex-shrink: 0;
  }
  .token-inline-bar.is-open {
    flex-direction: column;
    width: 100%;
    padding: var(--sp-4);
    background-color: var(--bg-secondary);
    border-top: 1px solid var(--border-color);
  }
  .token-inline-bar__input {
    width: 100%;
  }
}
```

---

## Task 10 — Fix `direction: rtl` layout reversal
**Priority:** P2 | **Effort:** 10 min | **File:** `style.css`

Around line 1503 in `style.css`, replace:
```css
/* BEFORE */
.use-case-row--reverse {
  direction: rtl;
}
.use-case-row--reverse > * {
  direction: ltr;
}

/* AFTER */
.use-case-row--reverse .use-case-row__content { order: 2; }
.use-case-row--reverse .use-case-row__figure  { order: 1; }
```

---

## Task 11 — Remove deploy-strip
**Priority:** P3 | **Effort:** 5 min | **File:** `en-gb/index.html`

Remove the entire `<div class="deploy-strip">` block (lines 82–91). The AWS strip below the hero communicates the same information more clearly.

---

## Implementation Order

Run tasks in this order (dependencies first):
1. Task 10 (RTL fix) — CSS baseline fix needed before layout work
2. Task 9 (mobile token bar) — CSS fix
3. Task 2 (enc-badge spacing) — CSS fix
4. Task 7 (privacy section — CSS) — CSS additions
5. Task 4 (patterns section — CSS) — CSS additions
6. Task 1 (screenshots) — wires in real images
7. Task 3 (PKI card) — content change
8. Task 4 (patterns section — HTML) — new section
9. Task 5 (view modes) — content addition
10. Task 6 (no-account note) — content addition
11. Task 7 (reorder sections) — HTML restructure
12. Task 8 (nav + vaults page) — new page
13. Task 11 (remove deploy-strip) — simplification

---

## What This Brief Does NOT Cover

- Full `/en-gb/vaults/` content strategy (stub is sufficient for this sprint)
- Three access modes full diagram (task 8 stubs this)
- i18n keys for new content (use hardcoded English for now, i18n pass to follow)
- Analytics or tracking changes (none in scope)
- Changes to production `sgraph_ai__website/en-gb/index.html`
