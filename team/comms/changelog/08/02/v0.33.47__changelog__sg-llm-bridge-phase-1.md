# Changelog — `sg.llm.*` bridge (Phase 1 + kernel-side accounting)

**Date:** 2026-08-02 · **Area:** Vault UI — `/en-gb/app/` app host (`app-shell.js`)
**Plan:** `team/roles/architect/reviews/08/02/v0.33.43__architect-review__sg-llm-kernel-capability-plan.md`
**Brief (how to use):** `team/comms/briefs/08/02/v0.33.47__brief__sg-llm-capability-how-to-use.md`

## What changed

Vault apps can now call an LLM through the host, with **the key never entering the app frame**.
Phase 1 of the plan, plus the kernel-side half of Phase 2 (accounting + budget enforcement).

### App-facing surface (injected bridge)

```js
sg.llm.available()            // {ok, reason?, model?, remaining:{calls,cost}}
sg.llm.models()               // [{id,name,pricing,context}] — policy-filtered
sg.llm.usage()                // {calls, promptTokens, completionTokens, cost, remaining}
sg.llm.chat(req, onToken?)    // → {content, model, finishReason, usage, cost, id, aborted}
sg.llm.cancel(requestId)      // promise from chat() carries .requestId
```

### Host-side chokepoint (`app-shell.js`)

Order is deliberate — **permission → consent → budget → model policy → call**:

| Gate | Behaviour |
|---|---|
| `permissions.llm.{chat,models,usage}` | Deny-by-default via existing `AppPermissions.can` (already parsed, previously unused). Refusal is `EPERM` and the upstream client is never constructed. |
| `available()` | **Ungated on purpose** — an app must be able to discover it cannot use the LLM in order to degrade. Reports `EPERM` honestly rather than lying about the key. |
| Consent | First `chat()` raises the HUD consent bar (`_consent('llm.chat')`); declining → `ECONSENT`. |
| Budget | `maxCallsPerSession` / `maxCostPerSession` enforced **host-side** → `EBUDGET`. Counted from the **shared** `VaultLlmLog`, so an app's spend and the vault chat panel's spend are one bill. |
| Model | `SGLlmConfig.modelAllowed` → `EMODEL`; `models()` returns only allowed ids. |
| `maxTokens` | **Clamped** to `limits.maxTokensPerCall`. The app asks; the policy decides. |

### Streaming over postMessage

`{__sgLlmDelta: id, delta, acc}` × N, then the terminal `{__sgCmdReply: id, ok, result}`.
Deltas are **coalesced in the host** on a ~50 ms timer and the **tail is always flushed** before
the terminal reply. Deltas carry only the increment (O(n), not O(n²)); the terminal reply is
authoritative, so an app that ignores `onToken` still gets the full `content`.

`cancel(id)` aborts the underlying fetch; the call settles as `EABORT` with partial text, and the
aborted call is **still recorded and costed** — stop must not feel like data loss or hide spend.

### Other

- `cmdReply` gained an optional `code` so apps branch on `err.code`, not message text.
- `_llmSession()` caches the resolved session per vault; **cache is checked before** the
  `SGLlmVault` availability guard (a resolved session is valid regardless of the resolver's
  presence, and re-unsealing per call was waste).
- HUD notice on the first LLM call per app session: *"This app is calling an AI model (…) with
  your vault's key."* An app spending money invisibly is what the HUD exists to prevent.
- `/en-gb/app/index.html` now loads `sg-llm-config.js`, `sg-llm.js`, `sg-llm-vault.js`,
  `vault-llm-log.js`.

## Files

**Changed:** `app-shell.js`, `en-gb/app/index.html`
**Docs:** `library/guides/vault-html/AUTHORING.md` (new "Calling an LLM" section + `llm` in the
API block), `library/skills/create-vault-apps/SKILL.md` (capability row, deny-by-default note,
frontmatter, verified date)

## Test impact

**SHOULD break (good failures) if reverted:** `test__app_shell_llm_bridge.js` (**new**, 45) —
drives the real handler with real postMessage frames:
- a grant-less app is refused `EPERM` **and the upstream client is never invoked**;
- declined consent → `ECONSENT`, client never invoked;
- over-budget → `EBUDGET`, counting spend recorded by *another* surface;
- disallowed model → `EMODEL`, request never reaches the client;
- `maxTokens: 999999` arrives at the client as the policy's `1000`;
- deltas are coalesced (< 1 frame per token) and concatenate to exactly the full reply;
- `cancel` aborts, settles as `EABORT`, and the aborted call is still in the ledger;
- **the API key appears in neither the injected bridge source, any delta frame, nor the reply.**

**Should NOT break (bad failures — investigate if they do):**
- Full vault unit suite green at commit time (`run-all.sh`).
- `test__bundle_freshness.js` — `app-shell.js` is **not** in the kernel bundle (only
  `app-permissions.js`, unchanged), so no rebuild was needed.
- Every other `__sgCmdType` handler — the `llm` branch is additive and returns early.
- Apps with no `permissions.llm` behave exactly as before.

## Deliberately NOT built (still PROPOSED)

- **CSP egress lockdown (§7).** App frames are not yet served with a restrictive `connect-src`,
  so an app can still reach a provider directly with its *own* key. The bridge protects the
  vault's key; it is not yet a boundary against all egress. **This is the gap that separates a
  convenience from a guarantee** and should be the next piece.
- **Phase 4 minted credentials** — the vault still stores a real key, so sharing a vault shares
  the ability to spend it.
- **ViV kernel parity** — served by the `/en-gb/app/` host only; nested kernels
  (`kernel-app-handlers.js`) do not relay `sg.llm.*`.
- Per-app limit overrides are read (`limitsFor(policy, appId)`) but no UI writes them yet.
