# Changelog — vault/token Test Harness Page

**Version:** v0.27.20
**Date:** 2026-05-12
**Author:** Explorer (Claude Code session claude/sgraph-send-test-harness-L7tPF)
**Brief:** `team/comms/briefs/05/12/v0.27.20__brief__vault-token-test-harness.md`

---

## Summary

New developer/debug page at `/en-gb/vault/token` — a multi-panel workspace for exercising
read-only token workflows, HKDF key derivation, and vault file inspection **without touching
any existing vault code**. Phase 1 only.

---

## Files Added

```
sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/en-gb/vault/token/
  index.html                — sg-layout shell + panel declarations
  vault-token-harness.js    — vt-vault-loader, vt-vault-frame, vt-crypto-lab, vt-storage-inspector
  vault-credentials.js      — parseCredential, resolveCredential, sessionStorage helpers
  vault-hkdf.js             — HKDF-SHA256 + AES-256-GCM inner encryption helpers
  vault-token-manager.js    — vt-token-manager (create/list/revoke RO tokens)
  vault-api-log.js          — vt-api-log (fetch proxy + scrolling call log)
```

---

## Files Modified

```
sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/en-gb/vault/peek/index.html
  — Added routing row for /en-gb/vault/token to the routing table (single line addition)

team/roles/librarian/reality/ui/index.md
  — Added v0.2.3 entry documenting the new page and components
```

---

## Zero Modified Files in _common/ or Existing Vault Components

**Constraint respected:** No modifications to `vault-shell.js`, `vault-header.js`,
or any file under `_common/`. The _common/ library scripts are imported read-only via
`<script>` tags in `index.html`.

---

## Test Impact

### Tests that SHOULD NOT break (good baseline)

- All existing vault tests (the existing vault code is untouched)
- Peek page routing table — still renders correctly; one new row added
- Any test that checks `_common/` files are unmodified

### Tests that are new (verify Phase 1 acceptance criteria)

- W1 — Vault loads as owner: Vault Loader form → Owner View iframe with `#[key]` hash
- W2 — Create RO token: Token Manager → POST /api/transfers/create → token in list, copy-link shows `#ro-[token]`
- W3 — Token resolves: Crypto Lab check-token → GET /api/transfers/check-token/*** → 200 valid
- W4 — RO frame placeholder: "Load in RO frame" → Phase 2 placeholder with copy/pop-out
- W5 — Revoke token: Revoke button → DELETE /api/transfers/delete/*** → revoked badge
- W6 — HKDF deterministic: same vault key → same first-8-hex of vault_secret_key
- W7 — Owner-namespace locked: `.vault/owner/*` fetch as owner → JSON; as RO → `{encrypted:true}`
- W8 — API log masks tokens: log entries show `***` not token strings

### Tests that WILL change (none for Phase 1)

No existing test coverage changes in Phase 1.

---

## Phase 2 Deferred Items

The following are explicitly out of scope for Phase 1 and require a separate brief:

1. Moving `vault-credentials.js` and `vault-hkdf.js` to `_common/js/lib/`
2. Adding `#ro-[token]` hash support to `vault-shell.js` (so RO View iframe actually loads)
3. Owner/RO badge in `vault-header.js`
4. Per-file access badges in the file list
5. File-open banners
6. "Open App" button in the vault header
7. Vault settings page "Read-only access links" section
