# Changelog — Sub-Vaults Phase 0 (convention reader + send-browse lazy-expand)

**Version:** v0.27.62
**Date:** 2026-05-25
**Author:** Dev/Architect (Claude Code session, branch `claude/exciting-brown-G2P9Z`)
**Trigger:** Dinis — start Phase 0 of the sub-vaults/external-resources plan, using approach (B)
  (enhance the shared `send-browse` for lazy-on-expand).
**Plan:** `team/roles/dev/reviews/05/25/v0.27.62__implementation-plan__sub-vaults-and-external-resources.md`

---

## Summary

First slice of the inline sub-vaults feature. Two pieces, both additive and backward-compatible:

1. **`vault-links.js`** (NEW) — the `*.link.json` convention reader: suffix recognition,
   minimal-link-file parse (returns `null`, never throws, on malformed / missing `ref_id`),
   mount path/label, and a localStorage child-key store ("save on this device"). No DOM, no secrets.
2. **`send-browse--v0.3.2.js`** (user v0.3.3 — the vault-authoritative copy) gains a **generic,
   backward-compatible lazy-on-expand hook**: a tree node carrying `_lazy === true` + `_folderPath`
   renders empty and, on first expand, calls `await this.dataSource.loadFolder(_folderPath)` then
   `_populateTree()` to re-render. `_subvault === true` (+ `_access`) renders a `🗄` glyph + `·ro` chip.

3. **`composite-data-source.js`** (NEW) — wraps the root `VaultDataSource` and splices sub-vaults in:
   `scan()` registers `*.link.json` (vault) mounts; `getTree()` hides the raw link file and presents
   each mount as a `_lazy`/`_subvault` folder node (splicing the child's prefixed tree once mounted);
   `loadFolder(mountPath)` opens the child vault **read-only** (key via localStorage→keyProvider) and
   splices; `getFileBytes` routes mounted paths to the child; writes/writable delegate to the root.
4. **`vault-shell.js`** — wraps the root data source in `CompositeDataSource` + `scan()`; Phase-0
   `keyProvider` is a `window.prompt` (no persistence; the link card with save-choices is Phase 2).
   `index.html` gains the two script includes.

This makes the end-to-end Phase 0 slice **code-complete**: a hand-authored `*.link.json` renders as
an inline sub-vault folder that opens read-only on expand. The splice logic is unit-verified (24/24);
the actual browser expand→prompt→render is not yet exercised (no browser here).

## Files Modified / Added

```
sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/_common/js/lib/links/vault-links.js                 (NEW)
sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/_common/js/adapters/composite-data-source.js        (NEW)
sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/index.html                                           (+2 script includes)
sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/_common/js/components/vault-shell/vault-shell.js     (wrap root in composite + scan + key prompt)
tests/unit/vault_ui/loader/test__vault_links.js                                                  (NEW, 26 assertions)
tests/unit/vault_ui/loader/test__composite_data_source.js                                        (NEW, 24 assertions)
tests/unit/vault_ui/loader/run-all.sh                                                            (+2 lines: run the new tests)
sgraph_ai_app_send__ui__user/v0/v0.3/v0.3.3/_common/js/components/send-download/send-browse--v0.3.2.js
  — _renderFolderNode: render _lazy nodes empty + data-lazy/data-folder-path; _subvault glyph + chip
  — _setupTreeListeners: folder-header click is async; lazy nodes loadFolder()+re-render on first expand
```

## Test impact

- **SHOULD pass / unaffected (no regression expected):**
  - All existing vault-UI loader unit tests + the pvp crypto KAT — **verified: pvp KAT 9/9, vault-links 26/26, composite-data-source 24/24**.
  - Share/Open UIs that use `send-browse`: their data sources never set `_lazy`/`_subvault`, so the
    new branches are inert and the rendered tree is byte-identical for them. (Only the v0.3.3 copy was
    patched; user v0.3.2 and open v0.4.0 copies are unchanged and should get the same patch when those
    products need sub-vaults.)
- **Verification gaps (cannot run here):**
  - **No browser/Playwright run** — the send-browse lazy-expand path and the `🗄`/chip rendering are
    syntax-checked and logic-reviewed, not exercised in a real browser. Needs a browser pass.
  - `_populateTree()` re-renders the whole tree on lazy expand, collapsing other open folders — a known
    Phase-0 limitation; Phase 2 should preserve/restore expansion.

## Notes

- Approach (B) chosen by Dinis ("now is the time to make these changes"). The enhancement is generic
  (any `_lazy` node), not sub-vault-specific, so it can serve other lazy-tree needs too.
- No secrets in `*.link.json` by design; child keys (Phase 0) live only in localStorage or a prompt.
