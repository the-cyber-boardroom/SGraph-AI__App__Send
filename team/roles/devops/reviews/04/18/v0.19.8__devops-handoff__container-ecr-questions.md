# Container ECR Deployment — Questions for Deploy Agent

**Version:** v0.19.8
**Date:** 2026-04-18
**From:** Explorer team (container packaging)
**To:** Agent handling ECR / AWS deployment

---

## Context

We are packaging SGraph Send as a Docker container (User Lambda only, no Admin). The container runs the existing `Fast_API__SGraph__App__Send__User` under uvicorn on port 8080. Auth uses the existing `SGRAPH_SEND__ACCESS_TOKEN` env-var fallback — no Admin Lambda dependency.

Full plan: `team/roles/devops/reviews/04/18/v0.19.8__devops-plan__container-deployment.md`

---

## What We're Building

- A Docker image containing the User FastAPI app + User/Vault/Workspace UIs
- Pushed to ECR (and eventually Docker Hub)
- Used to create a Docker-on-EC2 AMI via Packer

---

## What We Need From You

### 1. ECR Repository

We need an ECR repository for the container image.

**Proposed name:** `sg-send`
**Tags we'll push:** `latest`, `v0.19.8`, `v0.20.0`, etc.

**Questions:**
- Which AWS account should host the ECR repo? (Same account as the Lambda deployments — the one in `AWS_ACCOUNT_ID` secret?)
- Which region? The Lambdas use `eu-west-2`. Should ECR be in the same region?
- Should we use a private ECR repo or ECR Public (`public.ecr.aws`)?
- Lifecycle policy: we propose keeping last 10 tagged images and expiring untagged after 7 days. Acceptable?

### 2. GitHub Actions Secrets

The existing CI uses these secrets for AWS auth:
- `AWS_ACCOUNT_ID`
- `AWS_ACCESS_KEY_ID`
- `AWS_SECRET_ACCESS_KEY`
- `AWS_DEFAULT_REGION` (eu-west-2)

**Questions:**
- Can the existing IAM credentials push to ECR? Or do we need a new IAM policy / role with `ecr:GetAuthorizationToken`, `ecr:BatchCheckLayerAvailability`, `ecr:PutImage`, etc.?
- If using OIDC for GitHub Actions (recommended), is that already set up?

### 3. GitHub Actions Workflow

We'll add `.github/workflows/docker-build.yml`. The existing CI pattern uses reusable workflows (`ci-pipeline.yml` called by `ci-pipeline__dev.yml`, etc.).

**Questions:**
- Should the container build be a separate workflow (triggered on tags / manual dispatch), or integrated into the existing `ci-pipeline.yml`?
- Should we build on every push to `dev`, or only on version tags (`v*`)?
- The existing Lambda deploy uses `tests/deploy/` pytest tests. Should the container build follow the same pattern (pytest-driven), or is a standard GitHub Actions Docker workflow fine?

### 4. AMI — Packer Build

We plan to create an AMI that runs Docker and pulls the container image on boot.

**Questions:**
- Which AWS account/region for the AMI? Same as Lambda (`eu-west-2`)?
- Should the AMI be shared to other accounts or regions?
- Is there an existing Packer setup, or should we start from scratch?
- What instance type target? We're thinking `t4g.micro` (arm64) for cost. Does that match the existing infrastructure pattern?

### 5. Networking / Security Group

The container listens on HTTP port 8080. TLS is handled by an external proxy.

**Questions:**
- Is there an existing ALB, CloudFront distribution, or reverse proxy we should wire this into?
- Or is this standalone (direct EC2 access, user brings their own proxy)?
- Security group: just port 8080 inbound + SSH, or something more specific?

---

## Existing Infrastructure Summary

For reference, here's what currently exists (from the repo):

| Resource | Details |
|---|---|
| AWS Region | `eu-west-2` (London) |
| Lambda functions | User + Admin, deployed via pytest + osbot-aws |
| S3 buckets | `{account_id}--sgraph-send-transfers--{region}`, cache bucket, UI buckets |
| CloudFront | Distributions for send, vault, workspace UIs |
| CI/CD | GitHub Actions → pytest → osbot-aws deploy |
| IaC | None (no Terraform/CDK/SAM — all Python-driven) |
| Docker/ECR | **None currently** |

---

## What We'll Deliver

Once we have answers, we'll create:
1. `Dockerfile` + `.dockerignore`
2. `sgraph_ai_app_send/container/app.py` (4-line entry point)
3. `.github/workflows/docker-build.yml`
4. `packer/sg-send.pkr.hcl` (AMI template)
5. Container smoke tests

No changes to existing Lambda deployment. The container is a new, parallel deployment target.
