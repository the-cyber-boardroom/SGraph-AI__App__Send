# Dev Review: 25 May Briefs + Code Delivery 25–27 May

**Version:** v0.28.7 | **Date:** 28 May 2026 | **Role:** Dev
**Scope:** Human briefs `05/25/` (docs 489–493, v0.27.62) + code audit 25–27 May

---

## Summary Assessment

The 25–27 May delivery represents the largest single code burst since the v0.4.0 Share/Open split. Three parallel workstreams shipped:
- Public Vault Previews (PVP): 20+ commits, full feature set delivered
- Sub-Vaults (Phases 1–3): Web UI path complete, portable ro-links, external resource embeds
- App-Mode Permissions (Phases 1–4B): `AppPermissions` module + full consent flow + vault.create/unlink

All three workstreams are clean, well-changelocked, and the deferred items (vault.delete, Phase 6 reads-default-deny) have clear rationale documented.

---

## Code Changes Verified (25–27 May)

### Public Vault Previews

**Status: EXISTS — all core features shipped**

| Feature | Verified By |
|---------|-------------|
| Slug → transfer ID + read-only key derivation | `bd52920` and earlier pvp commits |
| Two access modes (/app/<id> + /app/<id>#key) | pvp commit series |
| OG meta-tag cards for social sharing | `001bcba` — bot UA routing to OG render |
| LARGE social card (1200×630, crop-to-fill + pad-to-fit) | `632126c`, `052e6a6`, `b327bcd` |
| Owner-controlled disclaimer badge + footer | `ec49126` |
| Copyable full-access link (masked + reveal) | `2ea09ea` |
| Embedded Settings tab editor with live preview | `e86649c`, `c1182ea` |
| Wrong-vault guard + loads existing preview into editor | `2b05e52` |
| Delete previews + per-vault management list | `11c19ac` |
| ro-token entry on /app/<id> key prompt | `13a567b` |
| Bookkeeping: `.vault/owner/public-previews/` | `3d2f816` (refactor to this path) |
| Pre-fill preview title + slug from vault name | `0b7b995` |

**Gap to verify: expiry/timing controls.** The arch brief specifies timing/expiry (a public preview works for a few days or a few accesses). No commit explicitly adds expiry logic. Dev to confirm: is expiry implemented in the current pvp code, or is it still PROPOSED?

### Sub-Vaults

**Status: EXISTS — Web UI (Phases 1–3)**

| Phase | What Was Built | Commit |
|-------|---------------|--------|
| Phase 1 | ro-links owner records — silent read-only open on any device | `e1e685f` |
| Phase 2 | Link card UI + vault-in-vaults user guide | `84b73af` |
| Phase 2 (ext) | External resources via controlled iframes | `8ae551a` |
| Phase 3 | Owner "Add link" UI in folder-tree controls | `32b9edb` |
| Phase portable | Portable ro-links records open on any device | `8ddc0d6` |
| Fix | First-click expand, drag-drop crash, view link file | `4fd5f89` |
| Fix | Sub-vault reads/lists via app bridge | `91055fc` |
| Fix | Preserve open folders on sub-vault lazy-load | `502bd37` |

**Test coverage note.** Sub-vault fixes went through test-drive; the changelog references browser verification (Playwright) but no new unit test count reported for this workstream. Dev to confirm test count impact for the QA domain.

### App-Mode Permissions

**Status: EXISTS — Phases 1–4B (Phase 5 deferred, Phase 6 deferred)**

| Phase | Feature | Tests |
|-------|---------|-------|
| 1 | `AppPermissions` module + security floor | 39 unit assertions |
| 2 | `app.json` grants + `fs.move/delete/mkdir` bridge verbs | Covered by Phase 1 unit tests |
| 3 | HUD privileges chip | Browser verification (no new unit tests) |
| 4A | HUD consent surface + `sg.ui.requestPermission` | Browser verification |
| 4B | `vault.create` + `vault.unlink` for apps | Needs browser + live backend verification |
| 5 | `vault.delete` | **DEFERRED** — needs owner-secret tier |
| 6 | Reads default-deny | **DEFERRED** — recorded decision, not scheduled |

**Unit test count change:** +39 assertions (Phase 1, verified). The global unit test count was ~602 before this session; now ~641. The QA domain count needs updating.

**app-shell.js growth concern.** Phase 4B added 97 lines to `app-shell.js` (already 1,154 lines at v0.2.3). At 1,250+ lines, this file is approaching the threshold where extraction to sub-modules becomes advisable. Flag for the Villager when hardening.

### Other Code (05/25–26)

| Feature | Commit | Status |
|---------|--------|--------|
| Vault header status pill redesign | `00cb59b` | EXISTS |
| Validated access-key entry | `00cb59b` | EXISTS |
| File actions moved to tree panel | `4916fca` | EXISTS |
| Deterministic RO-token ID resolution | `ab3d570` | EXISTS |
| `sg.history.*` API + sub-vault transparency | `e67fc95` | EXISTS |
| Write access token threading for in-app writes | `0a7dc2c` | EXISTS |
| Image src patching via VFS bridge | `1b6b621` | EXISTS |
| Vault key per-tab (access token stays shared) | `d22f720` | EXISTS |

---

## Brief Implementation Assessment

### Doc 490: Sub-Vaults Workflow

**Implementation estimate vs. what was built:** The brief estimated "Vault Web UI is the near-term build and priority." Phases 1–3 were shipped. Estimate was accurate.

**Remaining:** CLI clone-within-clone (P-248). No estimate given in the brief for this path; it is acknowledged as more complex. The brief says "handle storage location and resolve nested clones step by step" — this is a 1–2 week Dev session when it is prioritised.

### Doc 491: Vault Conventions and External Resources

**Phase 2 external resources shipped.** The brief described embedding external resources in controlled iframes; `8ae551a feat(vault-ui): sub-vaults Phase 2 — external resources via controlled embeds` delivered this.

**Naming decision still open.** Code uses `.link.json`; the brief left naming open for build time. Resolve with the Architect before adding more convention types.

**Plugin/app model (external apps via convention file + grant) — PROPOSED.** Not yet built. Moderate effort (1 session) when prioritised.

### Doc 492: Public Vault Previews

**Almost entirely shipped within the day.** 20+ commits from 05/25 onwards. The brief's acceptance criteria:

| # | Criterion | Status |
|---|-----------|--------|
| 1 | Optional public preview per vault | EXISTS |
| 2 | Deliberately public (server-side storage OK) | EXISTS |
| 3 | Deterministic public-vault-ID derivation | EXISTS |
| 4 | Convention JSON (title, description, thumbnail) | EXISTS |
| 5 | User-defined or random public IDs | EXISTS |
| 6 | Two access modes | EXISTS |
| 7 | Social-share meta-tag previews | EXISTS |
| 8 | Corporate disclaimers and support links | EXISTS |
| 9 | Timing/expiry controls | **VERIFY — may be PROPOSED** |
| 10 | Edit UX on vault settings page | EXISTS (embedded settings tab) |

### Doc 493: Talk to the Vault

**Not started in code.** The 05/26 vault-chat commits are architecture documents only (Track A+B pack, UX diagrams, state mockups). These are strong foundations. The brief says "wire it up, the components are there." Estimate:

| Track | Description | Effort |
|-------|-------------|--------|
| Track A | Simpler wiring path | ~1 session |
| Track B | Richer integration | ~2 sessions |

Dev recommendation: choose Track A first (simpler wiring, prove the concept), then extend with Track B if the initial version validates.

---

## Implementation Streams (Current Priority)

| Stream | Status | Effort | Owner |
|--------|--------|--------|-------|
| Talk to the vault (P-249) | PROPOSED — arch pack ready | ~1 session (Track A) | Dev |
| Owner-secret credential tier (unblocks vault.delete) | Design blocked | 1 design session + 1 build session | Architect + Dev |
| pvp expiry/timing controls | Unknown — verify first | TBD | Dev |
| Sub-vaults CLI access (P-248) | PROPOSED — low priority | ~2 sessions | Dev |
| Phase 6 reads-default-deny | Recorded decision — long-term | When apps declare fs.read | Dev |
| Convention file naming | Decision document needed | < 1 hour | Architect |
| app-shell.js extraction | Housekeeping — Villager | Medium | Villager |

---

## Test Gap Summary

| Area | Gap |
|------|-----|
| vault.create / vault.unlink | Needs browser + live backend verification (documented in Phase 4B changelog) |
| pvp slug derivation soundness | Cryptographic test / AppSec review pending |
| External resource iframe sandboxing | `sandbox` attribute verification |
| Sub-vault lazy-load under many nested vaults | Stress test — no automated coverage |

---

## Flags for QA

1. **Unit test count:** ~641 tests now (39 added by Phase 1 of app-mode permissions). Update `qa/index.md`.
2. **Phase 4B browser gap:** `vault.create`/`vault.unlink` need browser + live backend exercise. The changelog documents this explicitly — QA should add to the test plan.
3. **PVP expiry:** if expiry is PROPOSED, flag as an acceptance test gap (criterion 9 not met).
4. **iframe sandbox:** external resource embeds must be verified: no `allow-same-origin` for untrusted content.
