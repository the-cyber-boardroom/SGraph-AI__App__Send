# Dev Review: 19–21 April 2026 Briefs

**Version:** v0.21.8 | **Date:** 21 April 2026 | **Role:** Dev
**Scope:** docs 296–298 + code from merge (Docker container deployment)
**Previous review:** `team/roles/dev/reviews/04/20/v0.21.3__dev-review__briefs-16-18-apr.md`

---

## Executive Summary

This batch contains one significant architectural specification (`<sg-vault-picker>`), one research synthesis with concrete prompt engineering improvements, and one inter-team handoff whose implementation is already in `dev`. Code output from the merge is substantial — a full Docker container deployment target is now code-verified, including the EC2 provisioner CLI and 16 new tests.

---

## What Is Already Done (Merged to dev)

| Item | Location | Tests |
|------|----------|-------|
| `sgraph_ai_app_send__docker/` package | `sgraph_ai_app_send__docker/` | ✓ |
| `Fast_API__SGraph__Send__Container` with optional global auth | `…/Fast_API__SGraph__Send__Container.py` | 7 auth tests |
| Container `create_app()` factory | `…/app.py` | via container tests |
| `Dockerfile` (Python 3.12-slim, port 8080, MEMORY/DISK modes) | `…/Dockerfile` | ✓ |
| `provision_ec2.py` (`sg-send-ec2` CLI) | `…/provision_ec2.py` | (CLI surface, no real AWS) |
| `Enum__Storage__Mode` (MEMORY / DISK / S3) | `…/storage/Enum__Storage__Mode.py` | disk storage tests |
| Docker ECR build+push CI job | `.github/workflows/ci-pipeline.yml` | CI |
| `test_Container__App.py` — 9 tests | `tests/unit/container/` | ✓ |
| `test_Container__App__Auth.py` — 7 tests | `tests/unit/container/` | ✓ |

---

## Work Remaining — Prioritised Streams

### Stream 1: `<sg-vault-picker>` Component (Doc 297) — HIGH PRIORITY

This is the next high-impact build. It unblocks: profile page + credit activation (decision #1), auth for healthcare MVP (decision #12 path to production), and vault key management UX across all tools.

**Stage 1 — `localStorage` stand-in (independent minor version)**

| Task | Effort | Notes |
|------|--------|-------|
| `<sg-vault-picker>` component skeleton (create/open/save/share methods + events) | ~1 day | Vanilla JS, ~150 lines |
| Form UI: vault name input, vault key display, copy button, paste field | ~0.5 day | No PM API calls yet |
| `localStorage` read/write for vault credentials | ~0.5 day | Temporary stand-in |
| Unit tests for component logic | ~0.5 day | |

**Stage 2 — Credential Management API (independent minor version)**

| Task | Effort | Notes |
|------|--------|-------|
| `navigator.credentials.store(new PasswordCredential(...))` path | ~0.5 day | Chromium browsers |
| Hidden form + `autocomplete="new-password"` fallback | ~0.5 day | Safari / Firefox |
| `navigator.credentials.get({password: true, mediation: 'optional'})` for returning users | ~0.5 day | |
| Cross-browser Playwright tests (Chrome + Safari + Firefox) | ~1 day | Tests the form fallback heuristic |

**Stage 3 — Google Identity Services (optional, defer until after Stage 2)**

| Task | Effort | Notes |
|------|--------|-------|
| Load `accounts.google.com/gsi/client` browser script | ~0.5 day | CDN, no npm |
| Write JWT claims into vault as a JSON file | ~0.5 day | Stored inside vault, encrypted |
| "Welcome, Alice" UI personalisation | ~0.5 day | Reads from vault on open |

**Stage 4 — First adoption site**

Best candidate: infographic editor or PlaybookLM tools. Drop `<sg-vault-picker>` in as a vault-backed persistence layer. Low dev effort once the component exists.

**Before starting Stage 2, confirm:**
- `sg1.` prefix recommendation from Architect — does the vault layer strip it before processing?
- AppSec approval on shared-vault auto-save behaviour
- Sherpa's recovery UX design (optional prompt at vault creation)

---

### Stream 2: Infographic Generator — Nano Banana Improvements (Doc 296)

These are prompt engineering changes (low code risk) plus two feature additions (medium risk).

**Phase A — Prompt changes (no API change, can be applied immediately)**

| Change | Effort | Notes |
|--------|--------|-------|
| Rewrite prompt templates as narrative briefs | ~0.5 day | Edit template strings only |
| Quote all critical text strings in templates | ~0.5 day | Fixes most spelling glitches |
| Replace forbidden-word lists with positive layout descriptions | ~0.5 day | |
| Document under-8-element guidance in authoring docs | ~0.5 day | |

**Phase B — Tool feature additions (require code changes)**

| Feature | Effort | Notes |
|---------|--------|-------|
| Thinking Mode toggle (High/Dynamic) — `thinking_config` API param | ~1 day | If Gemini API is called directly |
| Reference-image parameter (`inline_data` content part) | ~2 days | New input surface + UI |
| Under-8-element schema validation heuristic | ~0.5 day | Template schema validation |

**Phase A should ship independently.** Phase B depends on whether the infographic tool has direct Gemini API access (not just a wrapped service). Check the tool's current implementation before estimating Phase B.

---

### Stream 3: EC2 Deployment — DevOps Handoffs (Doc 298 + merged code)

The `sg-send-ec2` CLI is written. What remains is AWS infrastructure that Dev cannot do without DevOps:

| Handoff Item | Owner | Status |
|---|---|---|
| Create ECR repository `sg-send` in `eu-west-2` | DevOps | Blocked on them |
| Add ECR push IAM permissions to CI credentials | DevOps | Blocked on them |
| Review access token in EC2 tags (production hardening) | Architect/AppSec | Flagged |
| `.github/workflows/docker-build.yml` (standard GHA Docker pattern) | Dev + DevOps | ECR repo needed first |
| Packer AMI template | Dev + DevOps | Post-MVP |

Dev action: write the `docker-build.yml` workflow stub (from the template in doc 298 §16) so it's ready when DevOps provisions the ECR repo.

---

## Risk Register

| Risk | Impact | Mitigation |
|------|--------|------------|
| `PasswordCredential` not available in Safari/Firefox | HIGH (user experience) | Form fallback handles it; needs Playwright cross-browser test to confirm |
| Vault key stored in EC2 tags with broad IAM access | MEDIUM (production only) | Dev/staging acceptable; add Secrets Manager path at production hardening |
| Infographic tool may not expose Gemini API directly | MEDIUM (Phase B delay) | Audit tool implementation before estimating Phase B |
| `sg1.` prefix on credential — vault layer may not handle it | LOW | Confirm with a two-line test before Stage 2 |

---

## Effort Summary

| Stream | Total Estimate | Dependencies |
|--------|----------------|--------------|
| `<sg-vault-picker>` Stage 1 | ~2.5 days | None — start now |
| `<sg-vault-picker>` Stage 2 | ~2.5 days | AppSec Q&A, Architect `sg1.` confirmation |
| Infographic Phase A (prompts only) | ~2 days | None — start now |
| Infographic Phase B (tool features) | ~3.5 days | Tool capability audit first |
| EC2 docker-build.yml stub | ~0.5 day | None |
| EC2 ECR provisioning | — | DevOps dependency |

---

*Dev — SGraph Send | 21 April 2026*
