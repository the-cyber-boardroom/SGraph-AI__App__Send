# Dev Review: Homepage Redesign Briefing — Alignment Confirmation
**Version:** v0.21.9 | **Date:** 2026-04-22 | **Role:** Dev
**Brief reviewed:** `team/comms/briefs/04/22/v0.21.9__brief__dev__homepage-redesign.md`
**Analysis reviewed:** `team/comms/briefs/04/22/v0.21.9__analysis__homepage-combined-findings.md`

---

## Status: ALIGNED — No Blocking Questions

All 11 tasks are understood, technically sound, and ready for implementation. Observations below are notes, not blockers.

---

## Task-by-Task Review

### Task 1 — Wire screenshots ✅
**Aligned.** Assets are confirmed present at `_common/images/sg-send/use-cases/`. The HTML structural change from `<div class="screenshot-placeholder">` to `<figure class="use-case-figure"><img></figure>` is correct. `loading="lazy"` is appropriate for below-fold images.

**Note:** The `.use-case-figure` CSS class must be added to `homepage.css` before the HTML change, or the layout will be unstyled briefly. Implement CSS first.

**Note:** The screenshot mapping in the brief assigns `04-upload-flow.png` to "Browser Automation." This is a reasonable approximation — ideally a Playwright/automation screenshot would be used, but none exists yet. Accept this mapping.

### Task 2 — Enc-badge spacing ✅
**Aligned.** Overriding `padding-block-start` in `homepage.css` (rather than editing the shared `style.css`) is the correct approach. The shared stylesheet's `.hero` rule sets `padding-block: var(--sp-24) var(--sp-16)` — the override in `homepage.css` applies only to this page, which is right.

### Task 3 — PKI card ✅
**Aligned.** PKI is confirmed in the reality document (vault key model, commit signing, per-vault scoping). The proposed card copy is technically accurate. The moved Sandbox callout preserves the information without claiming it exists.

**Note:** The callout currently contains two separate sentences concatenated: the Sandbox description + the seven-deployment-targets line. In the brief these are merged into one `<div class="callout">`. Separate them into two callouts or keep them distinct paragraphs — one for Sandbox, one for deploy targets.

### Task 4 — Interaction patterns section ✅
**Aligned.** CSS-only implementation with emoji actors is correct for IFD (zero framework dependencies). The `.pattern-card--featured` highlight on Agent→Human is a sensible choice — this is the most differentiated pattern (no competitor supports this use case).

**Note:** The section is positioned after the hero `</section>` and before the AWS strip. This places it between the hero and the primitives section — which is correct per the brief ("before the primitives section"). The AWS strip will then follow, and the primitives section after that. Confirm final section order:
1. Hero
2. Interaction Patterns (new)
3. AWS Strip
4. Three Primitives
5. Use Cases
6. Tools
7. Privacy (moved up)
8. Pricing
9. Open Source
10. Footer

### Task 5 — Vault view modes in primitive card ✅
**Aligned.** The replacement features list is accurate and adds "Three access modes: Online, CLI (sgit), API" which is a key clarification. This directly addresses the Ambassador's finding that API access is never mentioned.

### Task 6 — "No account required" hero note ✅
**Aligned.** Simple `<p class="hero-note">` after `.hero-ctas`. The copy is clear and accurate (share token recipients can open vaults without creating an account — confirmed in reality doc).

### Task 7 — Privacy before pricing + visual elevation ✅
**Aligned.** Section reorder is a pure HTML move (cut/paste the privacy section block). CSS changes to `.privacy-inline` are additive — `border-left` and font-size increases only. No risk of breaking other pages since `.privacy-inline` only appears on this page.

**Note:** The brief says `border-radius: 0 var(--radius-card) var(--radius-card) 0` for `.privacy-inline`. The existing `.privacy-inline` in `style.css` already sets `border-radius: var(--radius-card)`. The override should be applied in `homepage.css` to avoid touching the shared stylesheet.

### Task 8 — Vaults nav + stub page ✅
**Aligned.** Adding `<a href="vaults/">Vaults</a>` to the nav and footer is a small change with high strategic value. The vaults stub page should be created at:
`sgraph_ai__website/v0/v0.2/v0.2.0/en-gb/vaults/index.html`

The stub should:
- Use the same header/footer HTML pattern as other v0.2.0 pages
- Link the same CSS files (`../../../../_common/fonts/fonts.css`, `../../_common/css/style.css`)
- Contain a meaningful hero (not a blank placeholder)
- Cover: what a vault is, three access modes (Online / CLI / API), view modes, commit model, key types, sgit quickstart
- Be clearly labelled as an early/developing page if the content is incomplete

### Task 9 — Mobile token bar fix ✅
**Aligned.** The fix in `style.css` changes `display: none` to `display: flex` for the trigger at mobile breakpoint. This is a one-line change. The `.is-open` expanded state styling is preserved.

**Note:** This change is in the shared `style.css`, which affects all pages that use it. Review whether any other page uses `.token-inline-bar` before applying. (Answer: only the v0.2.0 pages include `token-bar.js` and use this component — safe to proceed.)

### Task 10 — Fix `direction: rtl` ✅
**Aligned.** Replacing `direction: rtl` with `order` properties on children is the correct semantic fix. The child elements of `.use-case-row` are `.use-case-row__content` and `.screenshot-placeholder` (which will become `.use-case-figure` after Task 1). The `order` override must reference both class names:
```css
.use-case-row--reverse .use-case-row__content { order: 2; }
.use-case-row--reverse .use-case-row__figure,
.use-case-row--reverse .screenshot-placeholder { order: 1; }
```

### Task 11 — Remove deploy-strip ✅
**Aligned.** The `deploy-strip` HTML block is self-contained (lines 82–91). Removing it leaves no dangling CSS (`.deploy-strip` rules in `homepage.css` can also be removed for cleanliness, but are not harmful if left).

---

## Implementation Sequence Confirmation

The brief's implementation order is correct with one clarification:

**Recommended order:**
1. CSS additions to `style.css` (Tasks 9, 10)
2. CSS additions to `homepage.css` (Tasks 2, 7, 4, 1)
3. HTML changes that use new CSS classes (Tasks 1, 3, 4, 5, 6)
4. HTML structural changes (Task 7 reorder, Task 11 removal)
5. Nav change (Task 8)
6. New vaults page (Task 8)

This order prevents any intermediate state where HTML references CSS classes that don't exist yet.

---

## Out of Scope Confirmations

Confirmed NOT in scope for this sprint:
- ❌ i18n keys for new content (hardcode English, i18n pass separately)
- ❌ Changes to `sgraph_ai__website/en-gb/index.html` (production homepage)
- ❌ Full vaults content strategy (stub page only)
- ❌ Any changes to application code (`sgraph_ai_app_send/`)
- ❌ Any changes to test files
- ❌ Any changes to Lambda functions

---

## No Pending Questions

All implementation details are clear. No design ambiguities. No technical blockers. Ready to implement.
