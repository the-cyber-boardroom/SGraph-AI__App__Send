# Architect Review — workspace.sgraph.ai Architecture

**Version:** v0.10.35
**Date:** 2026-03-03
**Role:** Architect
**Brief:** `team/humans/dinis_cruz/briefs/03/02/v0.10.14__dev-brief__document-transformation-workspace.md`
**Request:** Map out the best architecture for hosting the Document Transformation Workspace at `workspace.sgraph.ai`, maximising reuse of existing code and infrastructure from the other `.sgraph.ai` sites.

---

## 1. Executive Summary

The Document Transformation Workspace can be built as a **pure client-side application** that combines three things we already have — the vault's encrypted file system, the admin shell's component architecture, and the website's deployment pipeline — with one new thing: a browser-based LLM connection layer. No new backend endpoints are needed for Phase 1.

**Key architectural decision:** The workspace is a **new static site** deployed identically to `sgraph.ai`, pointed at the same backend as `send.sgraph.ai`. It reuses existing vault APIs, existing crypto libraries, and existing UI component patterns. The LLM calls go directly from the browser to provider APIs — the server never sees document content or LLM interactions.

---

## 2. Architecture Overview

```
workspace.sgraph.ai (CloudFront → S3)
         │
         ▼
┌────────────────────────────────────────────────────────┐
│  BROWSER                                                │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  workspace-shell (Web Component)                 │   │
│  │  ├── vault-panel (file browser, reused)          │   │
│  │  ├── document-viewer (source, left pane)         │   │
│  │  ├── document-viewer (transform, right pane)     │   │
│  │  ├── llm-chat-panel (prompt + response)          │   │
│  │  ├── llm-connection-manager (keys, providers)    │   │
│  │  └── debug-sidebar (API logger, events, msgs)    │   │
│  └─────────────────────────────────────────────────┘   │
│         │                    │                          │
│    Vault API calls      LLM API calls                   │
│    (via vault-api.js)   (direct from browser)           │
│         │                    │                          │
└─────────┼────────────────────┼──────────────────────────┘
          ▼                    ▼
   admin.send.sgraph.ai    LLM Provider APIs
   (existing vault endpoints)  (Anthropic, OpenAI,
                               Google, OpenRouter,
                               Ollama local)
```

### What's New vs. What's Reused

| Layer | Status | Source |
|-------|--------|--------|
| **Vault crypto** (RSA-4096 + AES-256-GCM hybrid) | REUSE | `vault-crypto.js` from User UI v0.1.7 |
| **Vault API client** | REUSE | `vault-api.js` from User UI v0.1.7 |
| **Vault index & folder structure** | REUSE | Existing data model (JSON index, folder objects) |
| **File type detection** | REUSE | `file-type-detect.js` from User UI v0.1.8 |
| **Markdown renderer** | REUSE | `vault-preview.js` from Admin UI v0.1.6 (or `markdown-parser.js` from v0.2.0) |
| **EventBus** | REUSE | `event-bus.js` from Admin UI v0.1.3 |
| **MessagesService** | REUSE | `messages-service.js` from Admin UI v0.1.3 |
| **API Logger** | REUSE | `api-logger.js` from Admin UI v0.1.3 |
| **Events Viewer** | REUSE | `events-viewer.js` from Admin UI v0.1.3 |
| **Messages Panel** | REUSE | `messages-panel.js` from Admin UI v0.1.3 |
| **CSS Design System** | REUSE | `admin.css` + `sg-brand.css` from Admin UI |
| **Deployment pipeline** | REUSE | `deploy-website.yml` + `deploy_static_site.py` |
| **Shell layout pattern** | ADAPT | `admin-shell.js` → `workspace-shell.js` |
| **Vault file browser** | ADAPT | `user-vault.js` → slim vault panel for workspace |
| **Document viewer** | NEW | Side-by-side panels with diff highlighting |
| **LLM connection manager** | NEW | Multi-provider API key management |
| **LLM chat panel** | NEW | Prompt input + streaming response display |
| **Prompt library** | NEW | Pre-built prompts, saved to vault |

**Score: 12 reuse, 2 adapt, 4 new** — roughly 70% of the infrastructure already exists.

---

## 3. Deployment Architecture

### Option A: Static Site on S3 + CloudFront (RECOMMENDED)

```
workspace.sgraph.ai
  → CloudFront distribution (new)
  → S3 bucket: {account-id}--static-sgraph-ai--eu-west-2
  → Path: websites/workspace-sgraph-ai/latest/
```

**Why this is best:**
- Identical pattern to `sgraph.ai` website deployment
- Reuses existing S3 bucket (just a new prefix)
- Reuses existing `deploy_static_site.py` script
- New CloudFront distribution + DNS record is the only AWS setup needed
- Static assets served at CDN speed (no Lambda cold starts for UI)
- Backend calls go to existing `admin.send.sgraph.ai` Lambda

**DNS:**
```
workspace.sgraph.ai → CNAME → d{xxx}.cloudfront.net
```

**CloudFront configuration:**
- Origin: S3 bucket at `websites/workspace-sgraph-ai/latest/`
- URL rewrite function: reuse existing `cloudfront/url-rewrite.js`
- Cache: HTML 300s, CSS/JS 86400s, images 604800s (same policy)
- TLS: ACM certificate (*.sgraph.ai or workspace.sgraph.ai)

### Option B: Lambda-Served (NOT recommended)

Serving workspace UI through the Lambda function (like send.sgraph.ai currently does) adds cold start latency for no benefit. The workspace has no server-side rendering needs.

### Repo Structure

```
sgraph_ai_app_send__ui__workspace/     # NEW directory
  index.html                            # Workspace shell
  _common/                              # Shared from admin/user UIs
    js/
      event-bus.js                      # From admin v0.1.3
      messages-service.js               # From admin v0.1.3
      vault-crypto.js                   # From user v0.1.7
      vault-api.js                      # From user v0.1.7 (adapted)
      file-type-detect.js               # From user v0.1.8
      markdown-parser.js                # From v0.2.0 or admin preview
    css/
      workspace.css                     # Workspace theme (extends admin.css)
  components/
    workspace-shell/
      workspace-shell.js                # Main shell (adapted from admin-shell)
    vault-panel/
      vault-panel.js                    # Slim file browser (adapted from user-vault)
    document-viewer/
      document-viewer.js                # Source/transform display panel
    llm-chat/
      llm-chat.js                       # Chat input + response stream
    llm-connection/
      llm-connection.js                 # Provider key management
    prompt-library/
      prompt-library.js                 # Pre-built + custom prompts
    debug/
      api-logger.js                     # From admin v0.1.3
      events-viewer.js                  # From admin v0.1.3
      messages-panel.js                 # From admin v0.1.3
```

---

## 4. Architectural Decisions

### AD-1: Backend API — Reuse Existing Vault Endpoints

**Decision:** No new backend endpoints for Phase 1. The workspace uses the same vault API that the user vault and admin vault-manager already use.

**Rationale:**
- Vault CRUD (create, file upload/download, index, folders) already exists — 10 endpoints on admin Lambda
- The workspace saves transformations as new vault files — same API
- Version history = saving a new file with a version suffix in the name — the vault index tracks it
- Prompt library = a special folder in the vault containing JSON files — no new API needed

**Trade-off:** This means the workspace requires an admin Lambda connection (same as vault). If we later want unauthenticated workspace access, we'd need to expose vault endpoints on the user Lambda too.

### AD-2: LLM Calls — Direct from Browser, No Proxy

**Decision:** All LLM API calls go directly from the browser to provider APIs. No server-side proxy.

**Rationale:**
- Zero-knowledge principle — server never sees document content or LLM prompts
- No CORS issues — all major LLM providers support browser-direct calls with API keys
- No server cost for LLM usage — the user pays their own API provider
- Simpler architecture — no new Lambda endpoints, no key management on server

**Verification needed:**
| Provider | Browser CORS | Verified? |
|----------|-------------|-----------|
| Anthropic (api.anthropic.com) | Supports `x-api-key` header | NEEDS VERIFICATION — Anthropic may block browser-origin requests |
| OpenAI (api.openai.com) | Supports `Authorization: Bearer` header | NEEDS VERIFICATION |
| Google (generativelanguage.googleapis.com) | Supports API key in query param | Likely works |
| OpenRouter (openrouter.ai) | Designed for browser use | YES — explicitly supports CORS |
| Ollama (localhost:11434) | Local, no CORS | YES — same-machine |

**Risk:** If Anthropic/OpenAI block browser-origin requests (common for security — API keys in browsers are visible to the user), we have two fallbacks:
1. **OpenRouter** as the universal gateway (one API, all models, explicitly browser-friendly)
2. **Lightweight CORS proxy** as a Lambda function (adds complexity but preserves zero-knowledge since the proxy only sees encrypted-in-transit API calls)

**RECOMMENDATION:** Start with OpenRouter + Ollama for Phase 1. Add direct provider support in Phase 2 after verifying CORS behaviour.

### AD-3: Vault API Origin — Cross-Origin to admin.send.sgraph.ai

**Decision:** The workspace at `workspace.sgraph.ai` calls vault APIs at `admin.send.sgraph.ai`.

**Requirements:**
- Admin Lambda CORS configuration must allow `workspace.sgraph.ai` as an origin
- Cookie-based auth (if used) must allow cross-origin credentials
- Alternatively, use a token-based auth that works cross-origin

**Current state:** The admin Lambda already handles CORS for the admin UI. We need to verify that `workspace.sgraph.ai` is added to the allowed origins list.

**Action item:** Check and update CORS configuration on admin Lambda to include `workspace.sgraph.ai`.

### AD-4: API Key Storage — In-Memory + Optional Vault Persistence

**Decision:** LLM API keys are held in memory during the session. Optionally, the user can save an encrypted config file to the vault containing their keys.

**Design:**
```javascript
// In-memory (default — secure, ephemeral)
llmConnectionManager.setKey('anthropic', 'sk-ant-...');

// Persist to vault (optional — user-initiated)
const config = { anthropic: 'sk-ant-...', openai: 'sk-...' };
const encrypted = await vaultCrypto.encrypt(publicKey, JSON.stringify(config));
await vaultAPI.storeFile(vaultKey, configGuid, encrypted);
// Index entry: { name: '.workspace-config.json', kind: 'file', _parent: rootGuid }
```

**Security:** Since vault files are encrypted with the user's RSA key, the API keys are protected at rest. They're only decrypted when the user opens the vault.

### AD-5: Shell Layout — Four-Panel Workspace

**Decision:** The workspace shell has four zones:

```
┌─────────┬──────────────────┬──────────────────┬─────────┐
│  VAULT  │    SOURCE DOC    │  TRANSFORMATION   │  DEBUG  │
│  PANEL  │    (left pane)   │   (right pane)    │  SIDE   │
│         │                  │                   │  BAR    │
│  Files  │  Loaded from     │  LLM output or    │  API    │
│  Folders│  vault, rendered │  edited version   │  Logs   │
│  Search │  with type-      │  with diff marks  │  Events │
│         │  aware viewer    │                   │  Msgs   │
│         │                  │                   │         │
│         ├──────────────────┴──────────────────┤         │
│         │  LLM CHAT PANEL                      │         │
│         │  [Prompt input] [Model selector]     │         │
│         │  [Send] [Save to Vault]              │         │
└─────────┴──────────────────────────────────────┴─────────┘
```

**CSS Grid:**
```css
.workspace-shell {
  display: grid;
  grid-template-areas:
    "header  header   header   header"
    "vault   source   transform debug"
    "vault   chat     chat      debug"
    "status  status   status   status";
  grid-template-columns: 240px 1fr 1fr var(--debug-width, 320px);
  grid-template-rows: 56px 1fr auto 28px;
  height: 100vh;
}
```

**Responsive behaviour:**
- Debug sidebar: collapsible (same as admin shell)
- Vault panel: collapsible to icon strip on mobile
- Source/transform: stack vertically on narrow screens
- Chat panel: full-width below documents

### AD-6: Document Viewer — Type-Aware Rendering

**Decision:** Reuse and extend the existing preview logic from `vault-preview.js` and `file-type-detect.js`.

| Type | Renderer | Source |
|------|----------|--------|
| Markdown | HTML render + source toggle | Extend `vault-preview.js` |
| HTML | Sandboxed iframe | NEW (with CSP restrictions) |
| Plain text | Monospace `<pre>` | Existing in `vault-preview.js` |
| JSON | Pretty-printed + collapsible tree | NEW |
| Images | `<img>` element | Existing in `vault-preview.js` |
| PDF | Embedded viewer (pdf.js) | NEW — Phase 2 |
| Code | Syntax highlighted | NEW — Phase 2 (highlight.js) |

**Phase 1 scope:** Markdown, plain text, JSON, images. These four types cover the primary workspace use case (document transformation) and all have existing renderer foundations.

### AD-7: Transformation Save Model

**Decision:** Transformations are saved as new files in the vault, not versions of the source file.

**Naming convention:**
```
Source:       report.md
Transform 1:  report__transform-001.md
Transform 2:  report__transform-002.md
Transform 3:  report__transform-003__executive-summary.md
```

**Metadata in vault index:**
```json
{
  "guid-transform-001": {
    "name": "report__transform-001.md",
    "kind": "file",
    "type": "text/markdown",
    "size": 4200,
    "_parent": "guid-transforms-folder",
    "_source": "guid-original-report",
    "_prompt": "Make more concise",
    "_model": "claude-3.5-sonnet",
    "_timestamp": "2026-03-03T14:30:00Z"
  }
}
```

**Why not versioning?** The vault doesn't have built-in file versioning. Adding versioning to the vault index would change the vault data model (which affects all vault users). Saving as separate files uses existing infrastructure. A "version timeline" view can be built client-side by reading `_source` references.

**Trade-off:** More files in the vault. Mitigated by putting transforms in a dedicated folder.

### AD-8: Prompt Library Storage

**Decision:** Prompts are stored as JSON files in a `.workspace/prompts/` folder in the vault.

```json
{
  "id": "prompt-improve-clarity",
  "name": "Improve clarity",
  "category": "document",
  "prompt": "Rewrite the following document for clarity. Preserve all facts and key points. Do not add new information. Output the full revised document.\n\n---\n\n{{document}}",
  "model_preference": null,
  "created": "2026-03-03T14:00:00Z",
  "builtin": true
}
```

**Pre-built prompts** are shipped with the workspace HTML (embedded in JS). **Custom prompts** are saved to vault. On load, the workspace merges built-in + vault-stored prompts.

### AD-9: LLM Response Streaming

**Decision:** Use Server-Sent Events (SSE) streaming where supported by the provider API.

**Why it matters for UX:** Document transformations can take 10-30 seconds. Without streaming, the user stares at a blank right panel. With streaming, text appears progressively.

**Implementation:**
```javascript
// Anthropic streaming
const response = await fetch('https://api.anthropic.com/v1/messages', {
  method: 'POST',
  headers: { 'x-api-key': key, 'anthropic-version': '2023-06-01' },
  body: JSON.stringify({ model, messages, stream: true })
});

const reader = response.body.getReader();
// Read SSE chunks, parse delta events, append to document viewer
```

All major providers support streaming (Anthropic, OpenAI, Google, OpenRouter). Ollama also supports streaming.

### AD-10: No New Lambda Endpoints (Phase 1)

**Decision:** Phase 1 requires ZERO backend changes.

**What the workspace needs from the backend:**
- Create vault → `POST /vault/create` ✅ EXISTS
- Store file → `POST /vault/file` ✅ EXISTS
- Get file → `GET /vault/file/{key}/{guid}` ✅ EXISTS
- Store index → `POST /vault/index` ✅ EXISTS
- Get index → `GET /vault/index/{key}` ✅ EXISTS
- Store folder → `POST /vault/folder` ✅ EXISTS
- Get folder → `GET /vault/folder/{key}/{guid}` ✅ EXISTS
- List folders → `GET /vault/folders/{key}` ✅ EXISTS

**Everything else is client-side:** LLM calls, prompt management, document rendering, diff computation, transformation saving.

### AD-11: Shared `_common` Directory — Copy, Don't Symlink

**Decision:** Copy shared JS/CSS files into the workspace's `_common/` directory. Do not attempt cross-site symlinks or shared CDN (cdn.sgraph.ai does not exist yet).

**Rationale:**
- Each site deploys independently to S3
- S3 doesn't support symlinks
- IFD methodology: each version is self-contained
- When `cdn.sgraph.ai` is built (future), we can move shared assets there

**Which files to copy:**
```
From admin UI v0.1.3:    event-bus.js, messages-service.js
From user UI v0.1.7:     vault-crypto.js, vault-api.js
From user UI v0.1.8:     file-type-detect.js
From admin UI v0.1.6:    vault-preview.js (adapt for document-viewer)
From v0.2.0:             markdown-parser.js
From admin UI v0.1.4:    sg-brand.css (adapt for workspace theme)
```

---

## 5. Security Considerations

### S-1: LLM API Keys in Browser

**Risk:** API keys are visible in browser DevTools.
**Mitigation:** This is an accepted risk — the user is connecting *their own* keys. The workspace is a tool, not a hosted service. The keys are only as exposed as they would be in any local development environment.
**Enhancement:** Recommend users create scoped/limited API keys for workspace use.

### S-2: Cross-Origin Vault Access

**Risk:** workspace.sgraph.ai calling admin.send.sgraph.ai across origins.
**Mitigation:** CORS allowlist on admin Lambda must include `workspace.sgraph.ai`. Cookie/credential handling must be explicit.

### S-3: HTML Rendering (XSS Vector)

**Risk:** User loads an HTML file from vault and it renders in the document viewer.
**Mitigation:** Render HTML in a sandboxed iframe with restrictive CSP:
```html
<iframe sandbox="allow-same-origin" srcdoc="..."></iframe>
```
Strip `<script>` tags and event handlers before rendering. This aligns with the AppSec findings from the gallery review (5 XSS vectors identified).

### S-4: LLM Response Injection

**Risk:** An LLM could return HTML/script in its response that gets rendered.
**Mitigation:** Always render LLM output as text/markdown, never as raw HTML. Use the existing markdown renderer which doesn't support `<script>` tags.

---

## 6. Phase Plan

### Phase 1 — MVP (1-2 weeks, Dev-led)

**Goal:** Load a vault file, send to LLM, see result, save transformation.

| # | Component | Effort | Dependencies |
|---|-----------|--------|-------------|
| 1 | `workspace-shell.js` (layout, routing, debug sidebar) | 2-3 days | Adapt from `admin-shell.js` |
| 2 | `vault-panel.js` (slim file browser) | 1-2 days | Adapt from `user-vault.js` |
| 3 | `document-viewer.js` (markdown, text, JSON, images) | 2-3 days | Extend `vault-preview.js` |
| 4 | `llm-connection.js` (OpenRouter + Ollama) | 1-2 days | NEW |
| 5 | `llm-chat.js` (prompt input, streaming response) | 2-3 days | NEW |
| 6 | 5 pre-built prompts (embedded in JS) | 0.5 days | — |
| 7 | Save transformation to vault | 0.5 days | Uses existing vault API |
| 8 | Deployment setup (S3 prefix, CloudFront, DNS) | 1 day | DevOps |

**Total Phase 1: ~10-15 dev-days**

### Phase 2 — Full Provider Support + Diff (2-3 weeks)

| # | Feature | Effort |
|---|---------|--------|
| 1 | Direct Anthropic/OpenAI/Google connections | 2-3 days |
| 2 | Diff highlighting (source ↔ transform) | 3-5 days |
| 3 | Custom prompt creation + vault storage | 1-2 days |
| 4 | Multi-document analysis (select 2+ files) | 2-3 days |
| 5 | PDF rendering (pdf.js integration) | 2-3 days |
| 6 | Code syntax highlighting (highlight.js) | 1-2 days |

### Phase 3 — Advanced (Future)

- Slide deck generation
- Audio briefing generation
- Collaborative editing
- Transformation timeline visualization
- Infographic generation via Gemini

---

## 7. Risks and Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|-----------|------------|
| **CORS blocking** — Anthropic/OpenAI reject browser-origin API calls | Blocks direct provider use | HIGH | Use OpenRouter as universal gateway for Phase 1 |
| **Admin Lambda CORS** — workspace.sgraph.ai not in allowlist | Blocks vault access | LOW | Config change, already know how to do it |
| **Large file handling** — documents > 6MB hit vault upload limit | Can't work with large PDFs | MEDIUM | Use chunked upload (API exists, UI needs work) |
| **Streaming complexity** — different SSE formats per provider | Dev effort | MEDIUM | Abstract behind `LLMProvider` interface |
| **Token costs** — users unaware of LLM API costs | User surprise | LOW | Show estimated token count before send |

---

## 8. Recommendation

**Build it.** The architectural alignment is strong:
- 70% of the code exists (vault, crypto, EventBus, debug panels, deployment)
- Zero new backend endpoints needed for Phase 1
- Zero-knowledge principle is preserved end-to-end
- The workspace naturally drives vault adoption (users need a vault to use the workspace)
- Deployment follows proven patterns (S3 + CloudFront, same as sgraph.ai)

**Start with OpenRouter + Ollama** for LLM connections. This gives immediate access to all major models (Claude, GPT-4, Gemini, Llama, Mistral) through one API while avoiding the CORS uncertainty of direct provider calls.

**The workspace is the vault's killer feature.** Without it, the vault is just encrypted Dropbox. With it, the vault becomes an encrypted workspace where documents are living, transformable assets.

---

*Review by: Architect (Explorer team)*
*Session: Claude Code — 2026-03-03*
