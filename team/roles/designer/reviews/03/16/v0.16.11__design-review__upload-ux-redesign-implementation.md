# Designer Review — Upload UX Redesign Implementation Guide

**Version:** v0.16.11
**Date:** 16 March 2026
**Role:** Designer (Explorer Team)
**Input:** `team/humans/dinis_cruz/briefs/03/16/v0.16.10__upload-ux-redesign-team-response.md`
**Grounded in:** Reality document v0.13.34, current design tokens (v0.2.0), existing `send-upload.css`

---

## 1. Design Assessment

The brief contains detailed ASCII mockups (Mockups A through E). This review translates those mockups into implementation-ready CSS specifications, component structure, and interaction details — everything the Dev needs to build pixel-accurate.

### Design System Context

The current design uses the **Aurora dark theme** (navy + teal accents). All new components must use existing design tokens from `design-tokens.css`. No new colour values — only new compositions of existing tokens.

| Token | Value | Usage in Redesign |
|-------|-------|-------------------|
| `--color-bg` | `#1A1A2E` | Page background |
| `--color-surface` | `#1E2A4A` | Card backgrounds |
| `--color-primary` | `#4ECDC4` | Active step, primary buttons, accents |
| `--color-text` | `#E0E0E0` | Primary text |
| `--color-text-secondary` | `#8892A0` | Hints, secondary labels |
| `--color-border` | `rgba(78, 205, 196, 0.15)` | Card borders, dividers |
| `--color-success` | `#4ECDC4` | Completed step checkmark |
| `--color-drop-zone-bg` | `#16213E` | Drop zone background |
| `--color-drop-zone-border` | `rgba(78, 205, 196, 0.25)` | Drop zone dashed border |
| `--accent-subtle` | `rgba(78, 205, 196, 0.12)` | Hover states, selected card |

---

## 2. Step Indicator Component

The step indicator appears at the top of the upload card across all three steps. It shows progress and allows visual orientation.

### Specification

```
┌──────────────────────────────────────────────────────┐
│  ● Upload        ○ Choose delivery       ○ Share     │
│  ━━━━━━━━━━━━━━━━╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌  │
│  Step 1 of 3                                         │
└──────────────────────────────────────────────────────┘
```

### CSS Implementation

```css
.step-indicator {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
}

.step-indicator__step {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    white-space: nowrap;
}

.step-indicator__step--active {
    color: var(--color-primary);
    font-weight: var(--weight-semibold);
}

.step-indicator__step--completed {
    color: var(--color-success);
}

.step-indicator__dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    border: 2px solid var(--color-text-secondary);
    background: transparent;
    flex-shrink: 0;
}

.step-indicator__dot--active {
    border-color: var(--color-primary);
    background: var(--color-primary);
}

.step-indicator__dot--completed {
    border-color: var(--color-success);
    background: var(--color-success);
    /* Show checkmark via ::after pseudo-element */
}

.step-indicator__line {
    flex: 1;
    height: 2px;
    background: var(--color-border);
    min-width: 20px;
}

.step-indicator__line--completed {
    background: var(--color-success);
}

.step-indicator__label {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    margin-top: var(--space-2);
}
```

### HTML Structure

```html
<div class="step-indicator">
    <div class="step-indicator__step step-indicator__step--active">
        <span class="step-indicator__dot step-indicator__dot--active"></span>
        <span>Upload</span>
    </div>
    <div class="step-indicator__line"></div>
    <div class="step-indicator__step">
        <span class="step-indicator__dot"></span>
        <span>Choose delivery</span>
    </div>
    <div class="step-indicator__line"></div>
    <div class="step-indicator__step">
        <span class="step-indicator__dot"></span>
        <span>Share</span>
    </div>
</div>
<div class="step-indicator__label">Step 1 of 3</div>
```

### Responsive Behaviour

On mobile (< 480px), step labels truncate:
- "Upload" → "Upload"
- "Choose delivery" → "Choose"
- "Share" → "Share"

```css
@media (max-width: 480px) {
    .step-indicator {
        gap: var(--space-1);
    }
    .step-indicator__step {
        font-size: var(--text-small);
    }
}
```

---

## 3. Step 1 — Drop Zone (Redesigned)

### Changes from Current

| Element | Current | Redesigned |
|---------|---------|-----------|
| Drop zone label | "Drop a file here or click to browse" | "Drop a file or folder" |
| Browse buttons | Centred, prominent, full border | Below drop text, secondary/ghost styling |
| File/Text toggle | Tab bar at top of upload area | Removed — text mode is a subtle link below |
| Size limit | "Maximum file size: 5.0 MB" | "Maximum upload: 10 GB" |
| Trust badge | Not present | "Zero cookies · Zero tracking · We cannot read your files" |
| Demo section | `<send-test-files>` component | "Try it first:" with demo links |

### Drop Zone CSS Updates

```css
/* Override browse buttons to secondary/ghost style */
.drop-zone__browse-buttons {
    display: flex;
    gap: var(--space-3);
    justify-content: center;
    margin-top: var(--space-4);
}

.drop-zone__browse-btn {
    background: transparent;
    border: 1px solid var(--color-border);
    color: var(--color-text-secondary);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-sm);
    font-size: var(--text-small);
    cursor: pointer;
    transition: border-color var(--transition), color var(--transition);
}

.drop-zone__browse-btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
}

/* Trust badge */
.trust-badge {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    margin-top: var(--space-4);
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-sm);
    background: rgba(78, 205, 196, 0.04);
}

.trust-badge__icon {
    font-size: var(--text-body);
    flex-shrink: 0;
}

/* Text mode link */
.text-mode-link {
    display: block;
    text-align: center;
    margin-top: var(--space-3);
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    text-decoration: none;
    cursor: pointer;
}

.text-mode-link:hover {
    color: var(--color-primary);
}

/* Demo section */
.demo-section {
    border-top: 1px solid var(--color-border);
    margin-top: var(--space-6);
    padding-top: var(--space-4);
}

.demo-section__title {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-3);
}

.demo-section__links {
    display: flex;
    gap: var(--space-4);
    flex-wrap: wrap;
}

.demo-section__link {
    font-size: var(--text-small);
    color: var(--color-primary);
    text-decoration: none;
    cursor: pointer;
    transition: opacity var(--transition);
}

.demo-section__link:hover {
    opacity: 0.8;
}

@media (max-width: 480px) {
    .drop-zone__browse-buttons {
        flex-direction: column;
        gap: var(--space-2);
    }

    .drop-zone__browse-btn {
        width: 100%;
        padding: var(--space-3) var(--space-4);
    }

    .demo-section__links {
        flex-direction: column;
        gap: var(--space-2);
    }
}
```

### Encrypting State (Step 1 sub-state)

When a file is dropped and encryption begins, the drop zone transforms:

```
┌──────────────────────────────────────────────┐
│  📁 project-docs/                            │
│  12 files · 18.5 MB · PDF, markdown, images  │
│                                              │
│  🔒 Encrypting in your browser...            │
│  ████████████████░░░░░░░░░░  47%             │
│                                              │
│  Your files never leave your device          │
│  unencrypted. Not now. Not ever.             │
└──────────────────────────────────────────────┘
```

```css
.encrypt-status {
    text-align: center;
    padding: var(--space-6);
}

.encrypt-status__file-info {
    font-size: var(--text-body);
    font-weight: var(--weight-semibold);
    color: var(--color-text);
    margin-bottom: var(--space-1);
}

.encrypt-status__file-meta {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
}

.encrypt-status__message {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    color: var(--color-primary);
    margin-bottom: var(--space-3);
}

.encrypt-status__trust-text {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    margin-top: var(--space-4);
    font-style: italic;
}
```

---

## 4. Step 2 — Delivery Choice Cards

### Card Layout

Cards are displayed in a responsive grid. Each card has: icon, title, description, and a "best for" hint. The recommended option gets a subtle highlight.

```css
.delivery-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-4);
    margin-top: var(--space-4);
}

.delivery-card {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-6);
    background: var(--color-surface);
    cursor: pointer;
    transition: border-color var(--transition),
                background var(--transition),
                transform var(--transition);
    text-align: left;
}

.delivery-card:hover {
    border-color: var(--color-primary);
    background: var(--accent-subtle);
    transform: translateY(-2px);
}

.delivery-card--selected {
    border-color: var(--color-primary);
    background: var(--accent-subtle);
    box-shadow: 0 0 0 1px var(--color-primary);
}

.delivery-card--recommended {
    position: relative;
}

.delivery-card--recommended::after {
    content: "★ RECOMMENDED";
    position: absolute;
    bottom: var(--space-3);
    right: var(--space-3);
    font-size: var(--text-micro);
    color: var(--color-primary);
    font-weight: var(--weight-semibold);
    letter-spacing: 0.05em;
    text-transform: uppercase;
}

.delivery-card__icon {
    font-size: 1.5rem;
    margin-bottom: var(--space-3);
}

.delivery-card__title {
    font-size: var(--text-body);
    font-weight: var(--weight-semibold);
    color: var(--color-text);
    margin-bottom: var(--space-2);
}

.delivery-card__desc {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin-bottom: var(--space-3);
}

.delivery-card__hint {
    font-size: var(--text-micro);
    color: var(--color-text-secondary);
    opacity: 0.7;
}

/* Back link */
.step-back-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    margin-top: var(--space-4);
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    cursor: pointer;
    text-decoration: none;
    border: none;
    background: none;
    padding: 0;
}

.step-back-link:hover {
    color: var(--color-primary);
}

@media (max-width: 480px) {
    .delivery-cards {
        grid-template-columns: 1fr;
    }
}
```

### Card Content Per File Type

| File Type | Icon | Title | Description | Hint |
|-----------|------|-------|-------------|------|
| Download (always) | 📥 | Send as download | Recipient gets a file to save to their device | Best for: large archives, backups |
| View (PDF, md, txt, html, images) | 👁️ | Let them view in the browser | Recipient reads/views directly, no download needed | Best for: documents, reports |
| Browse (folder, zip) | 📂 | Let them browse the folder | Recipient sees files in a browsable view with inline preview | Best for: sharing documents, reports |
| Gallery (images) | 🖼️ | Show as photo gallery | Recipient browses images in a gallery layout | Best for: photo sets, design assets |

### Interaction

- **Click** selects a card (`.delivery-card--selected`)
- **Click again** or **"Continue" button** below cards advances to Step 3
- Single available option: auto-advance after a brief highlight (300ms)
- Two options: show both, require explicit selection
- Three+ options: show all, require selection + Continue button

---

## 5. Step 3 — Share Credentials

### Default View (Simple Token)

The token is displayed prominently in a monospace box with a shuffle button. The share link is derived automatically.

```css
.share-credentials {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-6);
}

.share-code__label {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-2);
}

.share-code__display {
    display: flex;
    align-items: center;
    gap: var(--space-3);
}

.share-code__value {
    flex: 1;
    font-family: var(--font-mono);
    font-size: var(--text-h3);
    font-weight: var(--weight-bold);
    color: var(--color-primary);
    background: var(--color-bg);
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius);
    border: 1px solid var(--color-border);
    letter-spacing: 0.05em;
}

.share-code__shuffle {
    background: transparent;
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    padding: var(--space-2);
    cursor: pointer;
    font-size: var(--text-h3);
    color: var(--color-text-secondary);
    transition: color var(--transition), border-color var(--transition);
    line-height: 1;
}

.share-code__shuffle:hover {
    color: var(--color-primary);
    border-color: var(--color-primary);
}

.share-link {
    margin-top: var(--space-3);
}

.share-link__label {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-1);
}

.share-link__url {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-text);
    word-break: break-all;
}

.share-actions {
    display: flex;
    gap: var(--space-3);
    margin-top: var(--space-4);
    flex-wrap: wrap;
}

.share-actions__btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-sm);
    font-size: var(--text-sm);
    cursor: pointer;
    transition: background var(--transition), border-color var(--transition);
}

.share-actions__btn--primary {
    background: var(--color-primary);
    color: var(--color-bg);
    border: none;
    font-weight: var(--weight-semibold);
}

.share-actions__btn--primary:hover {
    background: var(--color-primary-hover);
}

.share-actions__btn--secondary {
    background: transparent;
    color: var(--color-text-secondary);
    border: 1px solid var(--color-border);
}

.share-actions__btn--secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
}

/* Alternative modes (collapsed by default) */
.share-alt-modes {
    margin-top: var(--space-4);
}

.share-alt-modes__toggle {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    cursor: pointer;
    background: none;
    border: none;
    padding: 0;
}

.share-alt-modes__toggle:hover {
    color: var(--color-primary);
}

.share-alt-modes__options {
    margin-top: var(--space-3);
    padding: var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    background: var(--color-bg);
}

.share-alt-modes__option {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-sm);
    cursor: pointer;
    transition: background var(--transition);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
}

.share-alt-modes__option:hover {
    background: var(--accent-subtle);
    color: var(--color-text);
}

.share-alt-modes__option + .share-alt-modes__option {
    margin-top: var(--space-1);
}

/* Geek mode (technical details) */
.geek-mode__toggle {
    margin-top: var(--space-3);
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    cursor: pointer;
    background: none;
    border: none;
    padding: 0;
    opacity: 0.6;
}

.geek-mode__toggle:hover {
    opacity: 1;
    color: var(--color-primary);
}

@media (max-width: 480px) {
    .share-code__value {
        font-size: var(--text-body);
    }

    .share-actions {
        flex-direction: column;
    }

    .share-actions__btn {
        justify-content: center;
        width: 100%;
    }
}
```

### "Change Code" Expanded State

When the user clicks "Change Code," the token display expands into the word picker:

```
┌────────────────┐  ┌────────────────┐  ┌─────────┐
│ Word 1         │  │ Word 2         │  │  5512   │
│ coal        ▾  │  │ render      ▾  │  │  auto   │
└────────────────┘  └────────────────┘  └─────────┘

Preview:  coal-render-5512
Security: ████████████████░░░░░  Good (~39.5B combinations)

[ 🔄 Shuffle ]  [ ✅ Use This Code ]
```

```css
.token-editor {
    display: flex;
    gap: var(--space-3);
    align-items: flex-start;
    margin-top: var(--space-3);
}

.token-editor__word {
    flex: 1;
}

.token-editor__word select,
.token-editor__word input {
    width: 100%;
    padding: var(--space-2) var(--space-3);
    background: var(--color-bg);
    color: var(--color-text);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
}

.token-editor__word label {
    display: block;
    font-size: var(--text-micro);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-1);
}

.token-editor__number {
    width: 80px;
}

.token-preview {
    margin-top: var(--space-3);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-text);
}

.token-entropy {
    margin-top: var(--space-2);
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-small);
    color: var(--color-text-secondary);
}

.token-entropy__bar {
    flex: 1;
    max-width: 200px;
    height: 4px;
    background: var(--color-border);
    border-radius: 2px;
    overflow: hidden;
}

.token-entropy__fill {
    height: 100%;
    background: var(--color-primary);
    border-radius: 2px;
    transition: width var(--transition);
}

@media (max-width: 480px) {
    .token-editor {
        flex-direction: column;
    }

    .token-editor__number {
        width: 100%;
    }
}
```

---

## 6. Trust Message Carousel

The carousel shows during both encryption (Step 1) and upload (Step 3) phases.

### Layout

```css
.trust-carousel {
    margin-top: var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    padding: var(--space-4) var(--space-6);
    background: var(--color-surface);
    min-height: 120px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden;
}

.trust-carousel__message {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.6;
    text-align: center;
    animation: carousel-fade-in 300ms ease;
}

@keyframes carousel-fade-in {
    from { opacity: 0; transform: translateY(8px); }
    to   { opacity: 1; transform: translateY(0); }
}

.trust-carousel__attribution {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    text-align: right;
    margin-top: var(--space-3);
    font-style: italic;
}

.trust-carousel__nav {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    margin-top: var(--space-3);
}

.trust-carousel__dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-text-secondary);
    opacity: 0.3;
    cursor: pointer;
    border: none;
    padding: 0;
    transition: opacity var(--transition);
}

.trust-carousel__dot--active {
    opacity: 1;
    background: var(--color-primary);
}

.trust-carousel__arrow {
    background: none;
    border: none;
    color: var(--color-text-secondary);
    cursor: pointer;
    font-size: var(--text-sm);
    padding: var(--space-1);
    transition: color var(--transition);
}

.trust-carousel__arrow:hover {
    color: var(--color-primary);
}

/* Sherpa tip below carousel */
.sherpa-tip {
    margin-top: var(--space-3);
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-sm);
    background: rgba(78, 205, 196, 0.04);
    font-size: var(--text-small);
    color: var(--color-text-secondary);
    display: flex;
    align-items: flex-start;
    gap: var(--space-2);
}
```

### Carousel Behaviour

- **Auto-rotate:** Every 4 seconds
- **Manual nav:** Clicking ◀ ▶ or dots pauses auto-rotation
- **Resume:** Auto-rotation resumes after 10 seconds of no interaction
- **Message transition:** Fade-in with subtle upward slide (300ms)
- **Message pool order:** Privacy policy statements first (messages 1-6), then interleave agent tips and feature highlights

---

## 7. Transition Animations

Smooth transitions between steps are essential for the wizard feeling guided rather than jarring.

```css
/* Step content transitions */
.step-content {
    animation: step-enter 300ms ease;
}

@keyframes step-enter {
    from {
        opacity: 0;
        transform: translateX(20px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Going back a step */
.step-content--reverse {
    animation: step-enter-reverse 300ms ease;
}

@keyframes step-enter-reverse {
    from {
        opacity: 0;
        transform: translateX(-20px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Card selection animation */
.delivery-card--selecting {
    animation: card-pulse 300ms ease;
}

@keyframes card-pulse {
    0%   { transform: scale(1); }
    50%  { transform: scale(1.02); }
    100% { transform: scale(1); }
}
```

---

## 8. Interaction Specification

### Step 1 → Step 2 Transition

1. User drops file/folder
2. Drop zone transforms to encrypting state (file info + progress bar + trust messages)
3. Encryption completes
4. Brief "Encrypted and ready" confirmation (800ms green flash)
5. Step indicator advances: Step 1 gets checkmark, Step 2 activates
6. Step 2 content slides in from right (300ms)

### Step 2 → Step 3 Transition

1. User clicks a delivery card
2. Card gets selected state (`.delivery-card--selected`)
3. If only one valid option: auto-advance after 300ms highlight
4. If multiple: user clicks card, then "Continue" button appears below cards
5. Step indicator advances
6. Step 3 content slides in from right

### Step 3 → Upload → Complete

1. Token is displayed with share link
2. User clicks "Copy Link" (primary action) — this triggers the upload
3. Upload progress replaces the share credentials area
4. Trust carousel shows during upload
5. On complete: share credentials return, with "Copied!" confirmation

### Back Navigation

- "← Back to upload" (Step 2 → Step 1): slides content right-to-left (reverse animation)
- Only available before upload starts
- Going back preserves the encrypted blob in memory (no re-encryption needed)

---

## 9. File Summary Display

When a file is selected/encrypted, show a consistent summary across steps:

```css
.file-summary {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    background: rgba(78, 205, 196, 0.04);
    border-radius: var(--radius-sm);
    margin-bottom: var(--space-4);
}

.file-summary__icon {
    font-size: 1.25rem;
    flex-shrink: 0;
}

.file-summary__name {
    font-weight: var(--weight-semibold);
    color: var(--color-text);
    font-size: var(--text-sm);
}

.file-summary__meta {
    font-size: var(--text-small);
    color: var(--color-text-secondary);
}

.file-summary__status {
    margin-left: auto;
    font-size: var(--text-small);
    color: var(--color-success);
    display: flex;
    align-items: center;
    gap: var(--space-1);
}
```

Content format:
- Single file: `📄 quarterly-report.pdf · 2.3 MB`
- Folder: `📁 project-docs/ · 12 files · 18.5 MB`
- With status: `✅ Encrypted and ready` or `📂 Folder view`

---

## 10. Accessibility Notes

| Element | Requirement |
|---------|-------------|
| Step indicator | `role="navigation"`, `aria-label="Upload progress"`, current step has `aria-current="step"` |
| Delivery cards | `role="radio"` within `role="radiogroup"`, keyboard navigable (arrow keys) |
| Trust carousel | `role="region"`, `aria-live="polite"` for auto-rotating messages, `aria-label="Privacy and security information"` |
| Shuffle button | `aria-label="Generate new share code"` |
| Copy button | `aria-label="Copy share link to clipboard"`, announce "Copied!" via `aria-live` |
| Progress bar | `role="progressbar"`, `aria-valuenow`, `aria-valuemin="0"`, `aria-valuemax="100"` |
| Back links | `role="button"` or `<button>`, not just styled text |

---

## 11. Implementation Priority for Dev

1. **Step indicator** — add to current upload component, visually only (no state change yet)
2. **Drop zone simplification** — remove text mode tabs, ghost-style browse buttons, add trust badge
3. **Encrypting state redesign** — file info + progress bar + trust text
4. **Step 2 delivery cards** — new render method, content detection logic
5. **Step 3 share credentials** — token display, copy/email/change actions
6. **Trust carousel** — separate component, plugged into encryption + upload phases
7. **Transitions** — CSS animations between steps
8. **Mobile responsive** — test all breakpoints, stack cards vertically

Each item is independently shippable and reviewable.

---

*Designer review produced 16 March 2026. All specifications use existing design tokens from v0.2.0 Aurora theme. No new colours or fonts introduced.*
