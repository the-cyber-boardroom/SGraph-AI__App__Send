# Librarian Review — Vault Web Code Index

**Version:** v0.33.44 | **Date:** 2026-07-24 | **Role:** Librarian
**Brief:** Dinis (session request) — build a comprehensive index of the Vault Web code
(content, features, to-dos) so agents picking up vault-web tasks are optimised and know
exactly what files to read.

---

## What was delivered

A new, maintained-in-place **code index** at `team/roles/librarian/code-index/vault-web/`:

| File | Content |
|------|---------|
| [`index.md`](../../../code-index/vault-web/index.md) | START HERE — orientation, ground rules, 5 mistake-preventing facts, reading paths per task type, definition of done |
| [`files.md`](../../../code-index/vault-web/files.md) | Per-file catalogue of the active tree (`v0/v0.2/v0.2.3/`, ~120 files, ~37k lines): globals, purpose, API surface, load-order constraints, incl. the complete `sg.*` bridge verb table |
| [`features.md`](../../../code-index/vault-web/features.md) | Feature → files map with code-verified status (engine, /vault browser, /app host, ViV kernel, sub-vaults, PVP, Vault Chat, loader/routing) |
| [`tests.md`](../../../code-index/vault-web/tests.md) | Full unit/integration/e2e map, harness design, exact run commands, CI wiring |
| [`todos.md`](../../../code-index/vault-web/todos.md) | Every known bug, limitation, browser-unverified gap, remaining ViV phase, and proposed item — each with its source document |

The whole v0.2.3 tree was read (4 parallel code-audit passes: core libs; app-shell/kernel;
browser components + pages; tests), not summarised from briefs.

## Reality corrections made (same-commit, per CLAUDE.md rule 4)

1. **Vault Chat Phases 1–4 SHIPPED** (commits `adc9d0f`, `60a2f6d`, `ad8e14d`, mid-June) —
   `ui/proposed/index.md` still marked P-263/P-264 as fully PROPOSED. Corrected in
   `reality/ui/index.md` + `reality/ui/proposed/index.md`.
2. **Bridge naming:** the app bridge exposes `sg.append.*` — no `sg.inbox.*` exists in code;
   there is no `sg.shell.*` namespace (print = `__sgPrintReq` RPC); `sg.git.*` is a
   deprecated alias of `sg.sync.*`. Older reality text used the stale names.
3. Pointers to the code index added to `reality/ui/index.md` and `reality/vault/index.md`;
   changelog entry added to `reality/changelog.md`.

## New verified findings (not previously recorded anywhere)

1. **6 loader unit test files are not wired into `run-all.sh`** — `npm run test:vault-unit`
   silently skips `test__ro_token_resolution`, `test__branch_index`, `test__write_batch`,
   `test__object_store_mem_cache`, `test__owner_secrets`,
   `test__app_permissions__vault_create_key`.
2. **The vault-chat unit suite is not CI-gated** (`_test-ui-vault.yml` never runs
   `test:vault-chat-unit` or the `*.smoke.mjs` scripts).
3. **4 e2e regression specs are entirely `test.skip`'d** + 1 orphaned integration test.
4. **Stale artefacts:** package-root `index.html` redirects to v0.2.1 (active tree is
   v0.2.3); root app title says "v0.2.2"; browse page title says "v0.2.1".
5. **Zero inline TODO/FIXME markers exist in the tree** — all open work lives in docs; the
   only code-level deferral markers are five ENOTIMPL/interim-posture comments in
   `app-shell.js`/`app-permissions.js` (catalogued in files.md §11.1).

## Maintenance contract

The code index is updated **in the same commit** as any Vault Web change (mirrors the
reality-document rule): files.md for new/moved files, features.md for feature status,
todos.md for opened/closed items. The index's "definition of done" section makes this part
of every vault-web task.

## Recommended follow-ups (small, high-value)

1. Wire the 6 missing unit tests into `run-all.sh` and add `test:vault-chat-unit` to
   `_test-ui-vault.yml` (test-infra only, no behaviour change — Villager-safe).
2. Fix the stale package-root redirect (v0.2.1 → v0.2.3) and page titles.
3. The send-browse `_write` chunk=8192 one-liner (todos.md §1).
