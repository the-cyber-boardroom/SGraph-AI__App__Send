# Master Index: 10 April 2026 — No New Briefs / Code Output Session

**Version:** v0.20.47 | **Date:** 10 April 2026 | **Librarian session:** Daily review (04/10)
**Scope:** `team/humans/dinis_cruz/briefs/04/09/` and `04/10/` — scan for new files since last index (04/09)
**Previous index:** `team/roles/librarian/reviews/04/09/v0.20.40__master-index__briefs-05-08-apr.md`
**Cumulative total before this session:** 251 documents
**New documents this session:** 0
**Cumulative total after this session:** 251 documents

---

## Scan Results

| Folder | New Files | Status |
|--------|-----------|--------|
| `briefs/04/09/` | — | Does not exist |
| `briefs/04/10/` | — | Does not exist |

**Result: No new briefs.** The last brief folder remains `04/08/`. No new documents require cataloguing or role review processing.

---

## Code Output Since Last Index (v0.20.40 → v0.20.47)

Although no new briefs arrived, two coding sessions landed significant code between 04/09 (evening) and 04/10. These are **code-verified EXISTS** items, not proposals.

### Session 01Bx9y725QHnEuaGunmi77UH — PLR + Print (04/09 late)

Responds to the Designer review `v0.20.39__designer-review__page-layout-ux-improvements.md` produced in the same dev cycle. Implements all P1 design deliverables plus structural PLR improvements.

| Commit | Type | What Changed |
|--------|------|-------------|
| `4ea4acd` | Feature | PLR: card frame, toolbar explicit dark colours, live theme selector on toolbar (6 schemes), columns vertical divider |
| `908d9dd` | Fix | PLR: banner outside card frame, dark-deck text visibility (#e8e8e8 hard-coded), theme selector persists after selection |
| `0dadaa1` | Fix | Print: remove `@page` margin override so browser dialog controls margins |
| `586eb8f` | Fix | Print: dark inline backgrounds stripped before printing, smaller screen margins, `.page-break` support |
| `9fd17d9` | Fix | Print: restore `@page margin: 1cm 1.25cm`; present mode `Cmd+P` uses `SgPrint.printHtml()` (not `window.print()`) |

**Files added / significantly changed:**

| File | Type | Description |
|------|------|-------------|
| `sgraph_ai_app_send__ui__user/v0/v0.3/v0.3.1/_common/js/sg-print.js` | **NEW** | `SgPrint` — zero-dependency print utility: `printHtml()`, `printMarkdown()`, A4 preview, SG/Send header/footer, dark bg strip, page breaks |
| `send-browse-v031--page-layout.css` | Addition (+560 net) | CSS for callout/stats/quote/author blocks; card frame; toolbar colours; columns divider; banner-on-dark class; dark-deck text fix |
| `send-browse-v031--page-layout.js` | Addition (+122 net) | Theme picker in edit mode; live theme selector on toolbar; banner-outside-card rendering; theme persistence fix |
| `page-layout-renderer.js` | Addition (+267 net) | `renderBanner()` exported; callout/stats/quote/author render logic |
| `send-browse-v031.js` | Modification | Present mode `Cmd+P` → `SgPrint.printHtml()` |
| `library/guides/content/page-layout-demo.json` | **NEW** | Demo JSON covering all PLR block types (hero, section, text, title, bullet-points, markdown, columns, cards, callout, stats, quote, author) |
| `library/guides/content/v0.20.18__guide__sg-send-page-layout-authoring.md` | Addition | Prop tables + JSON examples for callout, stats, quote, author |

**Designer review (already exists):** `team/roles/designer/reviews/04/09/v0.20.39__designer-review__page-layout-ux-improvements.md` — produced in the same session cycle, describes problem analysis, implementation decisions, and remaining backlog.

---

### Session 01MzzVzXBWitot9wWGhu8nVK — Vault Improvements (04/10)

Infrastructure improvements to the vault client library: correctness fix for vault HEAD discovery, large file support that removes the Lambda 6 MB ceiling, and UX refresh button.

| Commit | Type | What Changed |
|--------|------|-------------|
| `846b723` | Fix | Vault: `open()` reads branch index live (`bare/idx/...`) to get authoritative HEAD ref; falls back to HMAC-derived value for read-only/older vaults |
| `095f7c1` | Feature | Vault: large file support (>4 MB) via presigned S3 URL; `vaultReadLarge()` in sg-send.js; `loadLarge()` in vault-object-store.js; transparent fallback in memory/local mode |
| `4897e13` | Feature | Vault: `↺` refresh button in file tree toolbar; dispatches `vault-header-refresh`; handled by `vault-shell.js _onRefresh()` |

**Files changed:**

| File | Type | Description |
|------|------|-------------|
| `sg-send.js` | Addition (+23) | `vaultReadLarge(vaultId, filePath)` — presigned URL fetch path |
| `sg-vault-object-store.js` | Addition (+10) | `loadLarge(objectId)` — delegates to `vaultReadLarge` |
| `sg-vault-ref-manager.js` | Addition (+19) | `readBranchIndex(branchIndexFileId)` — decrypts + parses `branch_index_v1` |
| `sg-vault.js` | Addition (+209 net) | `open()` reads branch index; `getFile()` routes by size to `loadLarge()` |
| `vault-browse-edit.js` | Addition (+25 net) | Refresh button injected into tree toolbar |

---

## Code-Verified EXISTS Items Added This Session

The following are now verified as existing in code and added to the reality document:

| Feature | Notes |
|---------|-------|
| PLR: callout/stats/quote/author blocks (CSS + renderer) | 4 new block types with full CSS (light + dark mode) |
| PLR: theme picker in edit mode | `<select>` above textarea, 6 schemes, live re-render |
| PLR: live theme selector on toolbar | Left of source bar, no need to enter edit mode |
| PLR: card frame for page content | Dark panel wrapper with border-radius/shadow |
| PLR: columns vertical divider | Between adjacent cells, collapses mobile |
| PLR: toolbar explicit dark colours | Was nearly invisible |
| PLR: banner outside card frame | `renderBanner()` API; `.plr-banner--on-dark` class |
| PLR: dark-deck text fix | `#e8e8e8` hard-coded, not inherited from shell |
| PLR: theme selector persists | Holds selected value after re-render |
| `SgPrint` utility (`sg-print.js`) | `printHtml()`, `printMarkdown()`; A4 preview; dark-bg strip; page breaks; `@page 1cm 1.25cm` |
| Present mode `Cmd+P` uses SgPrint | Clean popup, not whole browse shell |
| `page-layout-demo.json` | Demo asset covering all block types |
| Page layout authoring guide update | callout/stats/quote/author documented |
| Vault: branch index HEAD discovery | `readBranchIndex()` in ref-manager; correct HEAD even for named branches |
| Vault: large file support (>4 MB) | Presigned S3 URL path; transparent fallback in memory mode |
| Vault: refresh button | `↺` in tree toolbar; dispatches refresh event to vault shell |

---

## Reality Document Status

**File:** `team/roles/librarian/reality/v0.16.26__what-exists-today.md`
**Action:** Updated this session — version bumped to v0.20.47, "Changes Since v0.20.40 (04/09 → 04/10)" section added with all code-verified items above.

---

## Role Reviews Produced This Session

| Role | File | Focus |
|------|------|-------|
| Dev | `team/roles/dev/reviews/04/10/v0.20.47__dev-review__plr-print-vault-sessions.md` | Implementation assessment of PLR+Print and Vault sessions; outstanding design backlog; open decisions |

**Designer review already existed:** `team/roles/designer/reviews/04/09/v0.20.39__designer-review__page-layout-ux-improvements.md` — no new designer review needed.

---

## Open Decisions (Carried Forward from v0.20.40)

1. **Profile page unblocks credit outreach** — still not built; ~50-user outreach blocked (doc 246)
2. **openrouter.ai egress allowlist** — needed before infographic generation works in Claude.ai sessions (from QA debrief 251)
3. **Ollama audio/video fail-fast vs auto-reroute** — still open (from 04/07)
4. **LiteRT-LM integration timing** — defer until HTTP server ships, still open
5. **Server-side zip versioning priority** — arch brief (doc 241) unslotted

## Open Decisions (New This Session)

6. **PLR context banner / "page identity" block** — Designer backlog P1; users don't understand what they're looking at. Needs `type: "banner"` or similar UX signal.
7. **PDF block in page-layout-demo.json** — demo omits `gallery`, `slides`, `pdf`, `image` blocks (require real vault files). P1 documentation gap.
8. **Vault large file API endpoint** — `GET /api/vault/presigned/read-url/{vault_id}/{file_id}` was added at v0.16.26 (see reality doc). The client-side large file support now uses this endpoint; confirm the backend has a matching test.

---

*Next session: watch for `briefs/04/10/` or later. Decisions 1 and 6 are highest priority.*
