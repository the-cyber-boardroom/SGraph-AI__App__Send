# ViV Phase 2 — What's Missing from the Original Plan

**Companion to:** the v2 ViV pack at `team/humans/dinis_cruz/briefs/05/vault-in-vault/version-2/01-architecture-review.md` (and its sibling specs `02..05`).

**Scope:** what the plan says should ship vs. what is actually code-verified today (v0.31.2). Cross-checked against `team/roles/librarian/reality/` and the loader test suite in `tests/unit/vault_ui/loader/`.

**Source-of-truth rule:** if a row says **PROPOSED**, it does not exist in code today. Tests passing on a synthetic harness do NOT make a feature exist for users.

---

## A. What IS shipped (code-verified)

| Capability | File(s) | Tests |
|---|---|---|
| `AppPermissions` floor + grant model + `vault.mount` capability | `app-permissions.js` | `test__app_permissions.js`, `test__app_permissions_vault_mount.js` |
| `Envelope` (signed always, encrypted when sensitive, mixed payloads) | `secure-channel-envelope.js` | `test__secure_channel_envelope.js` (39) |
| `SecureChannel` (port-anchored, PKI handshake, K1 mode, replay guard) | `secure-channel.js` | `test__secure_channel.js` (29) |
| `KernelMounts` (longest-prefix mount table) | `kernel-mounts.js` | `test__kernel_mounts.js` (14) |
| `KernelBroker` (per-kernel sidecar, policy + log, opaque entryId) | `kernel-broker.js` | `test__kernel_broker.js` (22) |
| Relay across `SecureChannel`-wired mounts | (composed) | `test__kernel_relay.js` (16) |
| `registerKernelVfsHandlers` — policy-enforcing kernel-side handlers | `kernel-app-handlers.js` *(v0.31.2)* | `test__kernel_app_handlers.js` (24) |
| `bootKernelOnPort` — testable bootstrap (handshake → vault.open → register) | `kernel-bootstrap.js` *(v0.31.2)* | `test__kernel_bootstrap.js` (13) |
| `sg.*` stub for the app frame (read/write/list/mounts/requestPermission) | `sg-app-stub.js` | `test__sg_app_stub.js` (13) |
| `KERNEL_SHELL_HTML` bundle for nested srcdoc spawn | `kernel-shell-bundle.js` (autogen by `scripts/build-kernel-shell-bundle.py`) | `test__bundle_freshness.js` (1) |
| Parent-side `_handleVfsViv` relay branch in app-shell for **read + write + list** | `app-shell.js` *(M3 added in v0.31.2)* | covered by relay tests; browser end-to-end PROPOSED |

---

## A-bis. CORRECTION (v0.31.2 follow-up commit)

An earlier draft of this doc listed B1/B2/B3 as missing. **That was wrong** — closer
reading of `app-shell.js` showed they ship. This section corrects the record; the
follow-up commit also extracted the parent orchestration into a testable module and
finished the M5 fix on the parent side.

| Item | Earlier (wrong) status | Actual status |
|---|---|---|
| **B1 mount** | "not assembled" | **SHIPS** — `__sgCmdType==='vault'` → `_mountChildVault` (app-shell.js) |
| **B2 unmount** | "no implementation" | **SHIPS** — `vault.unmount` → `_unmountChildVault` |
| **B3 HUD `ask`** | "nothing wires the HUD" | **SHIPS** — `KernelBroker.mediate` → `ui.prompt` → `_brokerPromptOnHud` → `app-hud.js requestConsent` |

What WAS genuinely wrong and is now fixed in the follow-up commit:

- **M5 was half-done.** The parent's `_spawnChildChannel` now passes `endpoint:
  this._sendEndpoint()` in the `secrets` payload. Before, the child always fell back
  to the hardcoded dev URL no matter where the parent ran.
- **Parent orchestration was untested.** Extracted into `kernel-parent.js`
  (`KernelParent`: mount table + broker + relay). app-shell delegates to it and
  supplies only the DOM-coupled `spawnChannel`. `test__kernel_parent.js` (21) drives
  a real `KernelParent` against a real child kernel (`bootKernelOnPort` + synthetic
  vault) — covering relay, the two-sided gate enforced by the *real* child, the
  floor surviving the relay, and broker `auto`/`ask`/`never` policies. The only
  untested remainder is the ~15-line iframe spawn (irreducibly DOM).

So B1-B3 move from "PROPOSED" to "SHIPS, now tested." The remaining genuine gaps are
the credential model (B5 below, the real one), the audit UI (B4), and the recorded
browser end-to-end (B9).

---

## B. What's MISSING (PROPOSED — not in code)

### B4 — Mounts list / broker log UI — ✅ SHIPPED (follow-up commit)

**Plan:** a "Mounts" panel listing each `BrokerEntry` with edge label, op, path, credential class, decision, result.

**Shipped as:** the **🔗 Mounts** tab in the /app debug pane. `viv-mounts-view.js`
(pure view-model, 25 unit tests) + `app-debug-mounts.js` (the pane component) +
app-shell installs `window._appDebug.vivProvider` = `() => ({ mounts: kp.list(),
entries: kp.broker.log() })`. Refreshes on each relayed op (`app-debug:bridge-call`).
Browser-verified by `test__viv_browser_e2e.spec.js` (B4 case).

**Why /app, not /vault:** the broker lives in the parent kernel on /app — that's where
the data is. A /vault surface would need cross-page persistence of the log (a separate
architectural decision); deferred. The pure `VivMountsView` is page-agnostic, so a
/vault embedding later is a small follow-on.

### B5 — Per-request elevation — ⚠️ MANDATED INVARIANT SHIPPED; issuance scheme deferred

**Plan:** three tiers — `none`, `standing`, `perRequest-rw`. Destructive verbs require elevation.

**Shipped (follow-up commit):**
- `viv-credential-tiers.js` — `TIERS` enum, `requiredTierFor(verb)`, `meets(a, b)`,
  fail-closed `gate({ verb, providedTier })` → throws `EUNDERPRIVILEGED` with
  `.required` + `.provided`. 26 unit tests.
- `kernel-parent.js relay()` runs the gate BEFORE mediation. `fs.delete` /
  `vault.delete` at `standing` tier → refused; no broker entry; channel never
  touched. With a `credential` field on the relay payload, tier flips to
  `perRequest-rw` and the same op proceeds. Verbs with unknown verbs default to the
  highest tier (fail-closed).
- Browser-verified by the e2e B5/B6 case.

**Still deferred (issuance):** how a per-request credential is *minted* (port-transfer
from Kernel-A / server-minted / user-typed), token format, revocation, issuance UI.
The gate above makes the *enforcement* end ship-ready; pair with the B10 issuance
decision before any real-data pilot.

### B6 — `vault.delete` owner-secret tier — ⚠️ FOLDED INTO B5

`vault.delete` is in the same verb table as `fs.delete` in `viv-credential-tiers.js`
(both require `perRequest-rw`). The destructive UX prompt is a separate UI piece
that belongs with the Phase 5 vaults page; the *invariant* (no destruction without
elevation) ships now.

### B7 — Monitored-mode child visibility — ⚠️ SHIPPED

**Shipped (follow-up commit):**
- `viv-monitor.js` — `MODES.CLOSED`/`OPT_IN` (frozen). `registerOnChannel(ch, broker, opts)`
  on the child registers a `broker.log` responder; default CLOSED → ECONSENT
  (no log leakage). `setMode('opt-in')` flips at runtime.
- `KernelParent.monitorChild(mountId, opts)` issues the request from the parent.
  Returns `{ mode, entries }` on opt-in; throws `ECONSENT` on closed; throws
  `ENOMOUNT` for unknown mounts.
- `kernel-bootstrap.js` wires it for every booted child kernel automatically. The
  child's broker + monitor handle are surfaced on the channel so app code (or its
  bootstrap caller) can toggle the mode.
- 16 unit tests + an e2e case. The full kernel-parent round-trip (real bootstrap,
  CLOSED → ECONSENT, then OPT_IN → entries returned) is the integration check.

### B8 — Phase 0.5 operational verification

**Plan:** before nesting, prove that the existing app-shell still works under `null`-origin + parity check of `window.sgVault` facade.

**Reality today:** the parity rewrite was not done. Today the /app page loads both `window.sgVault` (legacy) and `window.sg` (stub) — both work, but the stub is bypassed for /app's first-party iframes (which still use the legacy `__sgVfs*` postMessage protocol with mount-aware routing — M2/M3 fixes in v0.31.2).

**Effort:** medium. The plan called for re-expressing `window.sgVault` over the stub; today they coexist. Coexistence is fine for now, but the cleanup is owed.

### B9 — Phase 2 §7 browser end-to-end

**Plan:** a full SGVault-backed browser walkthrough of the driving case (clinician).

**Reality today:** synthetic unit tests pass; the browser walk has only been done by hand against the new bundle (no recorded run). The user guide in `v0.31.2__viv__user-guide-browser-test.md` is the step-by-step; a recorded Playwright run is still PROPOSED.

**Effort:** small if scripted via Playwright; needs two reachable dev vaults.

### B10 — Credential custody — ⚠️ MANDATED INVARIANTS SHIPPED; issuance scheme still deferred

The architect pack (`05-implementation-plan.md` §5) draws the line as **"product, not
mechanism"** — the *issuance* scheme is a human/AppSec decision. What the pack DOES
mandate is a fail-closed coupling rule: "real PHI requires either child-generated
keys or App-A already null. Parent-held child keys inside a same-origin App-A is the
one combination that exposes the child's secrets to any same-origin code — fine for
synthetic data, never for real data." That invariant ships now.

**Shipped (follow-up commit):**

- `viv-custody.js` — `MODES` enum (`parent-held` | `child-generated` | `user-entered`),
  `classifyAppFrameOrigin(sandboxSpec)`, fail-closed `check()` + `gate()`. 24 unit tests.
- `KernelParent.mount` runs `VivCustody.gate` BEFORE `spawnChannel`. The unsafe
  combination throws **`EUNSAFE_CUSTODY`** with no bring-up. New parent option
  `appFrameOrigin` (defaults `null-origin`) + `allowUnsafeSynthetic` (defaults
  `false`).
- `app-shell.js` classifies its own App-A iframe's sandbox automatically (today
  `allow-scripts allow-forms allow-same-origin` → `same-origin`); once Phase 3 drops
  `allow-same-origin` this auto-flips. The escape hatch is a single explicit global:
  `window.SG_VIV_ALLOW_UNSAFE_SYNTHETIC === true`. The trial `clinic.json` resolver
  now tags its creds `custody: 'parent-held'` so the gate is informed.
- `KernelParent.list()` carries `custody` on every mount; `sg.vault.mounts()` and the
  🔗 Mounts tab render the tag (parent-held shown in warning yellow).
- Browser-verified by `test__viv_browser_e2e.spec.js` (B10 case): real
  `KernelParent` in /app refuses parent-held + same-origin without spawning, allows
  child-generated through.

**Still deferred (B10 product half):** the *issuance* scheme — port-transfer (Kernel-A
holder), server-minted, or user-typed. The pack defers this and so do we; the gate
above makes deferral safe by default. **Pick before any real-data pilot.**

---

## C. Bugs identified and FIXED in v0.31.2

For completeness — these were holes between the v2 plan and the shipped code, surfaced by the audit at the start of this session, fixed in this commit:

| Tag | Issue | File(s) fixed |
|---|---|---|
| **H1** | Child kernel `vfs.*` handlers skipped `AppPermissions.isFloor` / `can` — the entire "two-sided gate" was missing in production code | `kernel-app-handlers.js` (new), `scripts/build-kernel-shell-bundle.py` |
| **M1** | `vault.push()` errors silently swallowed in the child kernel — silent data-loss on relayed write | `kernel-app-handlers.js` `_safePush` raises EUNREACH |
| **M2** | Parent applied its own `fs.write` grant to the `mounts/*` path BEFORE resolving the mount — relayed writes spuriously EPERM'd | `app-shell.js` (reorder mount-resolve before grant check) |
| **M3** | `__sgVfsListReq` had no mount-resolve+relay branch (read and write did) | `app-shell.js` (list relay added) |
| **M4** | Bootstrap (`KERNEL_BOOTSTRAP_JS`) was untested — relay test used a hand-rolled TestKernel that did NOT share code with the shipped child | `kernel-bootstrap.js` (new, testable) |
| **M5** | Endpoint hardcoded to `https://dev.send.sgraph.ai` | `kernel-bootstrap.js` reads endpoint from secrets payload |
| **M6** | `SecureChannel._dispatch` accepted any signature-valid envelope without checking it was addressed to THIS cid | `secure-channel.js` (cid check after unpack) |
| **L1** | `generateSignKeypair` had a dead then/catch fallback chain (only the catch branch ever ran) | `secure-channel-envelope.js` (simplified) |
| **L3** | Nothing checked `kernel-shell-bundle.js` was current vs. its sources — could ship stale bundle silently | `test__bundle_freshness.js` (new), `--stdout` flag on build script |
| **L4** | `_preHandshakeBuf` grew unbounded for unknown pre-handshake message types | `secure-channel.js` (bounded to 8 types × 8 entries) |

**Not fixed (L2):** `Envelope._canonicalParse` treats any `{__u8: "<string>"}` object as bytes. A legitimate JSON shaped that way would round-trip incorrectly. Edge case; deferred. The fix would be to add a tag byte (`{__u8b64: "..."}`) or a per-side marker.

---

## D. Suggested order for the remaining items

(B1/B2/B3 ship; M5-finish + `KernelParent` seam, **B9**, **B4**, **B10 mandated
invariants**, and **B5/B6/B7 mandated invariants** all landed in the follow-up commits.)

1. ✅ **B9** — browser end-to-end across a real null-origin srcdoc with browser crypto. DONE.
2. ✅ **B4** — mounts/broker-log viewer (🔗 Mounts debug tab). DONE.
3. ✅ **B10 (mandated invariants)** — fail-closed custody gate + visible custody tag + escape hatch. DONE.
4. ✅ **B7** — monitored-mode child visibility (`broker.log` protocol, CLOSED by default). DONE.
5. ✅ **B5/B6 (mandated invariants)** — credential-tier gate at the relay edge (`EUNDERPRIVILEGED`). DONE.
6. **B10 + B5 (product half — issuance scheme)** — pick port-transfer / server-minted / user-typed. AppSec input needed. **Gate before any real-data pilot.**
7. **B8** (Phase 0.5 cleanup) — finish re-expressing `window.sgVault` over the stub
   (coexistence works today; cleanup owed).
