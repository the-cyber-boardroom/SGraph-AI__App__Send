# Explorer Session Start — Designer + Dev + Ambassador + Alchemist

**Version:** v0.21.9 | **Date:** 21 April 2026 | **Session type:** Explorer multi-role
**Branch:** `claude/start-explorer-session-acc8G`

---

## Session Context

Starting from the Librarian's 04/21 daily review (v0.21.8). Key state:

- **Reality doc:** v0.21.8 — Docker container deployment EXISTS, auth MVP revised (vault key as credential, Cognito dropped)
- **Dev priorities:** `<sg-vault-picker>` Stage 1 (no dependencies, start now), Infographic Generator prompt improvements
- **Open decisions needing Dinis:** 6 questions on auth MVP design (see doc 297 §13)
- **Pending DevOps:** ECR repo + IAM permissions before Docker CI job can run

## Active Roles This Session

| Role | Focus this session |
|------|--------------------|
| **Designer** | `<sg-vault-picker>` UX design + landing page visual direction |
| **Dev** | `<sg-vault-picker>` Stage 1 implementation (localStorage stand-in) |
| **Ambassador** | Vault-key-as-credential messaging + "why not WeTransfer?" landing copy |
| **Alchemist** | Investor narrative update — Docker EXISTS, auth MVP redesign story |

## What Exists Today (relevant to this session)

From reality doc v0.21.8:

- `sg-vault.js` — full vault open/read/write/push/pull (AES-256-GCM, dual branch model)
- `vault-browse-edit.js` — edit/save/cancel for vault files, upload, new folder
- `vault-generate.js` — LLM infographic generation into vault
- `vault-header.js` — Push/Pull buttons with ahead/behind counts
- `sgraph_ai_app_send__docker/` — Container deployment target with optional auth middleware
- `<sg-vault-picker>` — **PROPOSED — does not exist yet** (Stage 1 is this session's build)

## Session Goals

1. **Ship:** `<sg-vault-picker>` Stage 1 — localStorage stand-in, form UI, events wired
2. **Design:** UX wireframe doc for all 4 vault-picker stages
3. **Write:** Landing page copy addressing "why not WeTransfer?" + vault-key positioning
4. **Update:** Investor narrative with Docker container milestone + auth MVP story

---

*All deliverables produced during this session are listed in the debrief at `../../../../../roles/` paths.*
