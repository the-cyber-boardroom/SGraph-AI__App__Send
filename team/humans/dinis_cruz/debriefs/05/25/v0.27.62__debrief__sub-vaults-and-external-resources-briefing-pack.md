# Debrief — Sub-Vaults & External Resources Briefing Pack

**version** v0.27.62
**date** 25 May 2026
**session** Architect (Explorer) — vault UI focus
**status** PROPOSED design. No application code changed. Deliverables are documents only.

---

## What you asked for

Start an Architect session on the vault UI code and, for the three attached briefs
(vaults-within-vaults, sub-vaults workflow, conventions & external resources):
see what we're trying to do; look at the related `.vault/*` + read-only-token work we
already shipped; review the code for the best way to build it; produce a plan with
ASCII wireframes; have the Designer review the UX; and collate everything into one
briefing pack covering architecture → design → implementation → testing/QA.

## What was delivered

1. **Architect briefing pack** (the main deliverable) —
   [`v0.27.62__briefing-pack__sub-vaults-and-external-resources.md`](../../../../../roles/architect/reviews/05/25/v0.27.62__briefing-pack__sub-vaults-and-external-resources.md)
   Covers the why/how/what, the code-verified foundations, all design decisions, ASCII
   wireframes, a phased implementation plan, a security analysis of the iframe boundary,
   and the testing/QA plan.
2. **Designer review** (commissioned during the session) —
   [`v0.27.62__designer-review__sub-vaults-and-external-resources-ux.md`](../../../../../roles/designer/reviews/05/25/v0.27.62__designer-review__sub-vaults-and-external-resources-ux.md)
   Endorsed the direction; flagged four ways the UI could mislead a user about who can
   read their data. All four are now folded into the pack's UX section (§4.6).
3. **Reality document updated** — added P-159…P-165 (this feature, all PROPOSED) to
   [`vault/proposed/index.md`](../../../../../roles/librarian/reality/vault/proposed/index.md).

## The key insight

**This is mostly wiring, not new invention.** Almost every primitive already exists and is
code-verified:
- `.vault/owner/*` double-encryption (HKDF owner-secret over the read-key layer) — reused verbatim for the pointer file.
- `SGVault.openReadOnly(...)` and `VaultLoader.openROToken(...)` already resolve a `{vault_id, read_key, ref_file_id}` reference and open a vault read-only. **A sub-vault "pointer" is structurally a stored read-only-token payload**, so opening one is the code path we already ship.
- Lazy folder loading is the model for "pre-initialise on access."
- `app.json`/`_page.json` is the convention-file template we stay uniform with.

## The main decisions (for your sign-off)

- **One convention file, `type`-discriminated:** `.vault/owner/pointers.json` holds an array of
  `{type: vault|link|video|app, …}`. This answers the briefs' open question ("shared format + type field?") and settles naming simply (no stacked extensions).
- **Owner-only by default:** the pointer file is double-encrypted, so only the parent vault's
  full-key holder can read/traverse it (correct for the doctor use case). A reader-visible tier is an additive option later.
- **External embeds are default-deny and a *different* code path from apps:** cross-origin
  iframe, locked-down sandbox, **no VFS bridge, no message listener**. This is the security crux — see pack §6.
- **Scope:** Vault Web UI first (read + traverse + owner add). CLI clone-within-clone and the
  selective app-grant channel are deferred to a Phase 4 (their own brief).

## What the Designer changed (and we adopted)

Granting data access is now behind an "Advanced" disclosure with a type-to-confirm that names
the site, folder, and file count (no more one-click leak); the live "granted" embed state is
drawn (amber, names the folder, attributes the choice to you, inline Revoke); the embed
guarantee banner lost its ✕ and is sticky; and read-only viewers see none of the owner-only
pointers (the labels themselves are sensitive). A "BEYOND THIS VAULT" divider + consistent
trust chips replace the ambiguous emoji.

## Recommended reading order

1. This debrief.
2. Briefing pack §1–3 (why/how/what + foundations + decisions).
3. Briefing pack §4 + the Designer review (UX).
4. Briefing pack §5–7 (implementation, security, testing) when you're ready to schedule the build.

## Open for your decision

- Owner-only vs. also shipping the reader-visible tier in v1.
- Whether `mode:"full"` (writable child vaults) is in v1 or deferred with create-child.
- Green-light Phase 1–3 (Web UI) now; Phase 4 (CLI + grant channel) as a follow-on brief.

## Honest status

- **No application code was written or changed.** This is a design/briefing deliverable only.
- All "EXISTS" claims in the pack are cited to `sgraph_ai_app_send__ui__vault/v0/v0.2/v0.2.3/` with file:line.
- Everything proposed is labelled PROPOSED and recorded in the reality document.
