# Debrief — sg.* docs audit, nav-interceptor fixes, sg.vfs.download, PDF-in-app finding

**Date:** 2026-07-31 · **Branch:** `claude/zealous-thompson-yDjFX` · **For:** Dinis Cruz
**Sessions covered:** the sg.* documentation review batch (24–31 Jul) through the
interceptor/download feature work (31 Jul).

---

## Executive summary

1. **Documentation audit** of every `window.sg.*` guide/skill in the repo, cross-checked
   against the shipped bridge, the live sgraph.ai library vault, the Dev Docs vault, and the
   shipped sgit CLI. Headline: the live library carries **no** vault-app API content; the
   agent-facing skills had three factually wrong claims; `sgit share/publish` are disabled in
   the shipped CLI but taught everywhere. All repo-side fixes executed.
2. **Architect review** verified the cross-vault in-app-navigation issue report against
   source (all claims confirmed; three findings stronger than the report), and its adopt-now
   proposals were **implemented**: interceptor opt-out (`data-sg-native` +
   `defaultPrevented`), bare-`#` clicks claimed in-frame, `location.hash` miss-fallback
   removed.
3. **`sg.vfs.download` shipped** — the sanctioned, host-fulfilled download API requested by
   the eu-ai-act downloads report, with a new default-deny `permissions.downloads` grant and
   HUD confirm. The app sandbox needs no `allow-downloads` token.
4. **PDF-in-app question settled empirically:** blob-URL `<iframe>` PDFs are **blocked** by
   Chromium inside the App Mode sandbox (verified by screenshot matrix). The working
   in-frame options are PDF.js-to-canvas or pre-rasterised pages; `sg.ui.preview` is
   PROPOSED as the one-call future answer.

## Deliverables (in recommended reading order)

### Reviews
- **Librarian review — sg.* docs vs live library sources of truth**
  [`v0.33.43__review__sg-star-docs-vs-live-library-sources-of-truth.md`](../../../../../roles/librarian/reviews/07/24/v0.33.43__review__sg-star-docs-vs-live-library-sources-of-truth.md)
  — the full audit + 7 prioritised recommendations (1–4 executed; 5–7 open, see "What's next").
- **Architect review — in-app nav / click interceptor proposals**
  [`v0.33.43__review__in-app-nav-click-interceptor-proposals.md`](../../../../../roles/architect/reviews/07/30/v0.33.43__review__in-app-nav-click-interceptor-proposals.md)
  — claim-by-claim verification with file:line cites; verdicts on all 5 proposals.

### Shipped code (commit `5eb3fe1`)
- Click-interceptor fixes (proposals 1–3) + `sg.vfs.download` + `permissions.downloads` +
  `app-hud.promptDownload`; kernel bundle regenerated; 8 new unit pins
  (`tests/unit/vault_ui/loader/test__app_shell_bridge_build.js`); full loader suite green.
- **Changelog with test-impact classification:**
  [`v0.33.43__changelog__click-interceptor-fixes-and-sg-vfs-download.md`](../../../../../comms/changelog/07/31/v0.33.43__changelog__click-interceptor-fixes-and-sg-vfs-download.md)

### Documentation updated (commits `46f8dcc`, `042e1a6`, `34e145b`, `5eb3fe1`, this one)
- **AUTHORING.md** (canonical `window.sg.*` reference)
  [`AUTHORING.md`](../../../../../../library/guides/vault-html/AUTHORING.md) — new sections:
  device-local state (`sg.state.*`), receiving messages (`sg.append.*` + `sg.on/off`),
  downloading files (`sg.vfs.download` + the `vfs.read` contract), displaying PDFs inline
  (the verified sandbox matrix); same-vault navigation rewritten for the fixed bare-`#`
  behaviour and the `data-sg-native` opt-out; expanded `sg.vault.*` verbs; banner refreshed.
- **create-vault-apps skill** [`SKILL.md`](../../../../../../library/skills/create-vault-apps/SKILL.md)
  — false `fetch()` claim fixed, external-links model updated, downloads/anchors/append/
  static-mode rows added, dead links fixed, canonical-source stamp added.
- **sgit skill** [`SKILL.md`](../../../../../../library/skills/use_sgit-and-vaults/SKILL.md)
  — `share send`/`publish` marked DISABLED (verified v0.14.27), `--read-key` read-only
  clone section added (the pattern for consuming public vaults), canonical-source stamp.
- **Reality indexes** — [`reality/ui/index.md`](../../../../../roles/librarian/reality/ui/index.md)
  (new dated section for the 07/31 ship; `sg.state.keys()` fix) and
  [`reality/vault/index.md`](../../../../../roles/librarian/reality/vault/index.md)
  (the `inbox → append` rename propagated).

### Cross-team replies
- **Reply to the downloads + nav-interceptor sessions** (answers all 5 downloads asks; includes
  the same-day PDF-preview correction):
  [`v0.33.43__reply__vault-app-downloads-and-nav-interceptor.md`](../../../../../comms/briefs/07/31/v0.33.43__reply__vault-app-downloads-and-nav-interceptor.md)

## Key decisions

1. **Downloads are host-fulfilled, not sandbox-widened.** No `allow-downloads` token for app
   frames; the save runs in the host document behind a HUD confirm (or the `downloads` grant).
   Less privilege in the frame, consent the app can't suppress.
2. **Bare-`#` anchors are now a host responsibility** (in-frame scroll); `location.hash`
   assignment stays banned (srcdoc spec, not fixable host-side).
3. **`data-sg-native` / `defaultPrevented`** is the sanctioned interceptor opt-out — apps
   stop needing window-capture workarounds.
4. **PDF inline rendering:** blob-iframe is a dead end in the sandbox (empirically verified,
   contrary to my own earlier reply); PDF.js-to-canvas is the sanctioned in-frame approach.
   `sg.ui.preview(path)` — host-side overlay reusing `vault-file-preview` — is **PROPOSED,
   does not exist yet**.

## What's next (open, needs your call)

- **Publish the vault-html guides + skills into the library vault** (librarian review rec 5)
  — needs a write token for the `pmcv9tfe` vault, out-of-band.
- **Skills manifest** (rec 6, partially done via stamps) — a `skills-manifest.json` in the
  library vault so agents can check skill freshness with one read-key fetch.
- **Interceptor proposals 5a/4/5b** (query-string delivery, `sg.app.onDeepLink`, HUD
  `?node=` addressing) — PROPOSED; 5a fixes a live navigation break for `?query` hrefs.
- ~~**`sg.ui.preview(path)`**~~ — **SHIPPED same day** (no-consent host quick-look overlay;
  see [`v0.33.43__changelog__sg-ui-preview.md`](../../../../../comms/changelog/07/31/v0.33.43__changelog__sg-ui-preview.md)).
  **`sg.ui.copy(text)`** remains PROPOSED.
