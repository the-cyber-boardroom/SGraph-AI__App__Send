# ═══════════════════════════════════════════════════════════════════════════════
# SGraph Send — Admin Lambda — Local Development Environment
# Copy this to .local-server.env and fill in your values
#   cp .local-server.env.example .local-server.env
#
# The run script (scripts/admin__run-locally.sh) loads this automatically
# .local-server.env is gitignored — never commit secrets
# ═══════════════════════════════════════════════════════════════════════════════

# ---------------------------------------------------------------------------
# Option A: Stub mode (no AWS credentials needed)
# Enables /metrics/* routes with canned sample data
# Use this to develop the admin UI without AWS access
# ---------------------------------------------------------------------------
SGRAPH_SEND__METRICS_USE_STUB=true

# ---------------------------------------------------------------------------
# Option B: Real AWS mode (requires AWS credentials + IAM permissions)
# Comment out METRICS_USE_STUB above and uncomment + fill in below
# Requires: cloudwatch:GetMetricData, cloudwatch:GetMetricStatistics,
#           cloudwatch:ListMetrics on the caller's IAM identity
# ---------------------------------------------------------------------------
# SGRAPH_SEND__METRICS_USE_STUB=
# SGRAPH_SEND__CLOUDFRONT_DISTRIBUTION_ID=E1XXXXXXXXXX
# SGRAPH_SEND__LAMBDA_USER_NAME=sgraph-send-user-dev
# SGRAPH_SEND__LAMBDA_ADMIN_NAME=sgraph-send-admin-dev
# SGRAPH_SEND__S3_TRANSFERS_BUCKET=sgraph-send-dev-transfers
# SGRAPH_SEND__S3_CACHE_BUCKET=sgraph-send-dev-cache
# SGRAPH_SEND__S3_FILTER_ID=all-requests
# SGRAPH_SEND__AWS_REGION=eu-west-2
