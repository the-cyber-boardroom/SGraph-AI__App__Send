name: Deploy Vault UI to S3

# Publishes vault static UI files to S3 for CloudFront distribution.
# CloudFront serves vault.sgraph.ai from S3.
#
# Deploy is driven by the same deploy_static_site.py used by the website
# and send UI — consistent cache-control, IFD versioning, and invalidation.
#
# S3 path isolation by branch/environment:
#   websites/sgraph-vault/dev/    — dev branch (automatic)
#   websites/sgraph-vault/main/   — main branch (automatic)
#   websites/sgraph-vault/prod/   — production (manual dispatch)
#
# Steps:
#   1. Generate i18n locale pages from en-gb/ templates + i18n/*.json
#   2. Sync content (source + generated) to S3 under the environment prefix
#   3. Invalidate CloudFront cache
#
# Environments:
#   - dev:        dev.vault.sgraph.ai  (default — push to dev branch)
#   - main:       main.vault.sgraph.ai (push to main branch)
#   - production: vault.sgraph.ai      (manual dispatch)
#
# Secrets per environment:
#   - SGRAPH_VAULT__UI_S3_BUCKET                   (S3 bucket for vault static files)
#   - SGRAPH_VAULT__CLOUDFRONT_DISTRIBUTION_ID      (production CF distribution)
#   - SGRAPH_VAULT__DEV__CLOUDFRONT_DISTRIBUTION_ID (dev CF distribution)

on:
  push:
    branches:
      - dev
      - main
    paths:
      - 'sgraph_ai_app_send__ui__vault/v0/v0.1/**'
      - 'scripts/generate_vault_i18n_pages.py'
      - '.github/workflows/deploy-ui-vault.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'dev'
        type: choice
        options:
          - dev
          - production

concurrency:
  group: "ui-vault-deploy-${{ inputs.environment || github.ref_name || 'dev' }}"
  cancel-in-progress: false

env:
  AWS_REGION  : eu-west-2
  UI_VERSION  : v0.1.1
  SOURCE_DIR  : sgraph_ai_app_send__ui__vault/v0/v0.1/v0.1.1

jobs:
  deploy:
    name: "Publish Vault UI to S3 + CloudFront (${{ inputs.environment || github.ref_name || 'dev' }})"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: ${{ inputs.environment || (github.ref_name == 'main' && 'main') || 'dev' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Generate i18n locale pages
        run: python scripts/generate_vault_i18n_pages.py --version ${{ env.UI_VERSION }}

      - name: Check AWS credentials
        id: check-creds
        run: |
          if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ] || [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then
            echo "has-credentials=false" >> $GITHUB_OUTPUT
            echo "WARNING: AWS credentials not configured — skipping deployment"
          else
            echo "has-credentials=true" >> $GITHUB_OUTPUT
          fi

      - name: Configure AWS credentials
        if: steps.check-creds.outputs.has-credentials == 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id     : ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key : ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region            : ${{ env.AWS_REGION }}

      - name: Resolve deployment environment
        id: env
        run: |
          DEPLOY_ENV="${{ inputs.environment || github.ref_name }}"
          if [ "$DEPLOY_ENV" == "production" ]; then
            echo "deploy_env=prod" >> $GITHUB_OUTPUT
            echo "dist_id=${{ secrets.SGRAPH_VAULT__CLOUDFRONT_DISTRIBUTION_ID }}" >> $GITHUB_OUTPUT
            echo "Deploying to PRODUCTION (vault.sgraph.ai) → S3: websites/sgraph-vault/prod/"
          elif [ "$DEPLOY_ENV" == "main" ]; then
            echo "deploy_env=main" >> $GITHUB_OUTPUT
            echo "dist_id=${{ secrets.SGRAPH_VAULT__DEV__CLOUDFRONT_DISTRIBUTION_ID }}" >> $GITHUB_OUTPUT
            echo "Deploying to MAIN → S3: websites/sgraph-vault/main/"
          else
            echo "deploy_env=dev" >> $GITHUB_OUTPUT
            echo "dist_id=${{ secrets.SGRAPH_VAULT__DEV__CLOUDFRONT_DISTRIBUTION_ID }}" >> $GITHUB_OUTPUT
            echo "Deploying to DEV (dev.vault.sgraph.ai) → S3: websites/sgraph-vault/dev/"
          fi

      - name: Deploy Vault UI to S3
        if: steps.check-creds.outputs.has-credentials == 'true'
        run: |
          python scripts/deploy_static_site.py \
            --site sgraph-vault \
            --version ${{ env.UI_VERSION }} \
            --source-dir ${{ env.SOURCE_DIR }} \
            --bucket ${{ secrets.SGRAPH_VAULT__UI_S3_BUCKET }} \
            --region ${{ env.AWS_REGION }} \
            --deploy-env ${{ steps.env.outputs.deploy_env }} \
            --cloudfront-distribution-id ${{ steps.env.outputs.dist_id }} \
            --skip-validation \
            --version-file sgraph_ai_app_send/version
